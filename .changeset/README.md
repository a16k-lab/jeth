# Changesets

This directory stores pending JETH release notes. Each Markdown file created by
`npm run changeset` records the affected package, the SemVer bump, and a concise
description for the changelog.

Do not edit `config.json` as part of an ordinary feature or compiler fix. See
[`docs/guide/releasing.md`](../docs/guide/releasing.md) for the release workflow.
