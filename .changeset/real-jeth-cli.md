---
"jeth": minor
---

Replace the development CLI wrapper with a packaged, tested `jethc` executable supporting strict arguments, filesystem imports, multi-contract output and selection, project configuration, structured JSON, standard JSON, stable exit codes, artifact metadata, and an exact solc 0.8.35 dependency.
