# dependencies / build output
node_modules
dist
*.tsbuildinfo

# graphify generated knowledge graph (regenerate with `graphify update .`)
graphify-out

# lockfiles + misc
package-lock.json
.DS_Store

# docs, markdown, and the managed .claude/ skill files are left to their own style
# (Prettier is scoped to code: src/, test/, examples/)
*.md
docs
.claude

# a test-generated fixture: map-array-fuzz-struct-with-fixed-array-field.test.ts rewrites
# this file from an inline constant on every run, so formatting it just causes churn.
test/_gen4e2b_struct-with-fixed-array-field.jeth

# JETH read-only methods deliberately allow parameters on `get`, which the
# TypeScript formatter rejects even though the JETH parser supports it.
examples/CloneFactory.jeth
