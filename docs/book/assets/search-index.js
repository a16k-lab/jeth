window.JETH_SEARCH_INDEX = [{"title":"JETH Documentation","group":"Guides","text":"JETH language documentation ! a16k organization logo ../assets/a16k-avatar-logo.png JETH is a statically typed smart-contract language for the EVM. Its syntax is a strict TypeScript-shaped subset, while its values, arithmetic, storage, ABI, calls, errors, and execution model follow EVM and Solidity semantics. Built by @a16k-lab https://github.com/a16k-lab . JETH compiles source into typed IR and Yul, then uses solc to optimize and assemble creation/runtime bytecode. The compiler also emits ABI and storage layout information. !WARNING JETH is pre-release software. The compiler has an extensive differential test suite and zero known miscompiles at the current revision, but it has not completed the production release and independent audit gates. Guides The HTML book is organized as a structured language manual: - Start here: installation, a first contract, a language tour, and examples. - Language: declarations, contract structure, types, locations, expressions, control flow, functions, globals, and JETH-specific features. - Contracts: inheritance, private members, modifiers, interfaces, calls, libraries, events, errors, and panics. - Internals: ABI encoding, storage layout, compiler APIs, CLI behavior, and artifacts. - Advanced: contract creation, CREATE2 clones, proxies, beacons, diamonds, facets, and namespaced storage. - Security: smart-contract risks and the compiler-correctness model. - Reference: syntax, differences, diagnostics, supported paths, and limits. The short tour is optional. The detailed language chapters are the primary documentation. Recommended path 1. Installation and first contract getting-started.md 2. Structure of a contract language/contract-structure.md 3. Value types language/value-types.md 4. Reference and composite types language/reference-types.md 5. Expressions and operators language/expressions.md 6. Statements and control flow language/control-flow.md 7. Functions language/functions.md 8. Interfaces and external calls language/interfaces-and-calls.md 9. Contract ABI internals/abi.md 10. Storage layout internals/storage-layout.md 11. Contract creation and clones advanced/contract-creation-and-clones.md 12. Security considerations security/considerations.md Core ideas TypeScript-shaped source, EVM behavior u256 , address , mappings, storage slots, ABI words, EVM calls, and revert data are real language concepts. JavaScript number , implicit coercion, objects, promises, and garbage collection do not exist at runtime. Native contract syntax - A bare leaf class is a contract. - A bare field is storage. - A leading makes a field or method private to its declaring class. - External<T and Payable<T expose ABI entries. - Visible<T exposes a state, constant, or immutable getter. - Ordinary methods are internal. - A get class method is read-only; the analyzer infers pure or view . - A static method is declared pure. - Initialized static fields are constants; uninitialized ones are immutables. Safety-first compilation Accepted source must have a sound analyzer and code-generation path. When a complex copy, location, ABI, or aliasing shape is not proven, JETH rejects it at compile time. A clean over-rejection is safer than accepted wrong bytecode. HTML book Run the local documentation build: Open docs/book/index.html directly or serve docs/book/ with any static HTTP server. The output has structured Guides navigation, search, light/dark themes, responsive pages, and a dedicated .jeth highlighter. It has no hosted documentation runtime dependency. Documentation status These pages are versioned with the compiler. SUPPORTED.md ../../SUPPORTED.md remains the exhaustive source-shape acceptance matrix. Files directly under docs/ are engineering and audit records and can describe historical implementation states.","path":"./"},{"title":"Getting started","group":"Guides","text":"Getting started Requirements - Node.js 22 - npm - A local clone of the JETH repository The compiler package is currently private and pre-release. Use the repository entry point until a versioned jethc package and standalone binary are published. Install and verify Compile the bundled counter example: Write artifacts to a directory: The current CLI can print generated Yul, ABI JSON, creation/runtime bytecode, and storage layout: Create a contract Create Hello.jeth : Compile it: The output directory contains ABI, creation bytecode, runtime bytecode, and Yul. The exact output format is not stable until the public CLI milestone is complete. Read JETH syntax correctly - Integer literals use BigInt syntax: 1n , not 1 . - u256 means an EVM uint256 , not a JavaScript number. - A bare class is a contract. - A bare field is storage. - A method returning External<T is externally callable. - A get method is read-only; the compiler infers pure or view . - A method without External<T or Payable<T is internal. - Arithmetic is checked unless it is inside unchecked: { ... } . Next steps - Take the language tour language-tour.md . - Browse progressively larger examples examples.md . - Use the language reference language-reference.md for exact syntax. - Check SUPPORTED.md ../../SUPPORTED.md before relying on an advanced shape. - Read security considerations security/considerations.md and the compiler correctness model security/compiler-correctness.md before deploying anything.","path":"guides/getting-started/"},{"title":"Language tour","group":"Guides","text":"Language tour This tour introduces JETH by building a small vault. It focuses on the concepts that differ most from TypeScript and Solidity. State and external methods balances is persistent contract storage. Payable<void exposes a payable entry point. External<u256 exposes a nonpayable ABI entry. Because balanceOf uses get and reads storage, its ABI mutability is inferred as view . Checked arithmetic Unsigned overflow and signed overflow revert with Solidity-compatible panic data. Division by zero and invalid enum conversions also use the matching panic codes. Use unchecked only when wrapping is deliberate and proven safe: Internal helpers Methods without an exposure return wrapper are internal: Internal calls can use trailing defaults and named arguments. External ABI calls always provide the full selector-defined argument list. Types JETH includes Solidity-compatible integer widths, signed integers, booleans, addresses, fixed bytes, dynamic bytes, strings, mappings, arrays, fixed arrays, struct-like object aliases, enums, and tuples. Brand<T creates a nominal value type with no runtime or ABI overhead. Different brands cannot be mixed accidentally without an explicit conversion. Arrays and iteration JETH supports dynamic arrays T and fixed arrays Arr<T, N . for...of desugars to indexed EVM-oriented iteration and supports break , continue , and return where the element shape is supported. Events and custom errors Events use Solidity-compatible topics and ABI data. Custom errors use canonical selectors and ABI-encoded arguments. Modifiers and reentrancy protection @nonReentrant uses EIP-1153 transient storage and consumes no persistent slot. It is one defense, not a replacement for checks-effects-interactions, access control, invariant tests, and review. Exhaustive control flow An enum switch without default must cover every member. Adding an enum member therefore makes incomplete switches fail at compile time. Where to go deeper - Language reference language-reference.md - Functions language/functions.md - Contracts and ABI contracts-and-abi.md - JETH-specific features language/jeth-features.md - Complete supported matrix ../../SUPPORTED.md","path":"guides/language-tour/"},{"title":"Examples","group":"Guides","text":"Examples The examples/ directory contains runnable source programs. Start with small contracts and move to composite ABI/storage cases. Suggested learning order 1. Counter.jeth ../../examples/Counter.jeth : state, checked arithmetic, and an external getter. 2. Arith.jeth ../../examples/Arith.jeth : integer operations and panic behavior. 3. Control.jeth ../../examples/Control.jeth : branches and loops. 4. Globals.jeth ../../examples/Globals.jeth : msg , block , and tx data. 5. Events.jeth ../../examples/Events.jeth : event topics and data. 6. Errors.jeth ../../examples/Errors.jeth : revert strings and custom errors. 7. Arrays.jeth ../../examples/Arrays.jeth : dynamic arrays. 8. FixedArrays.jeth ../../examples/FixedArrays.jeth : fixed arrays. 9. Structs.jeth ../../examples/Structs.jeth : struct construction and storage. 10. Nested.jeth ../../examples/Nested.jeth : nested state shapes. 11. Vault.jeth ../../examples/Vault.jeth : a larger end-to-end contract. 12. CloneFactory.jeth ../../examples/CloneFactory.jeth : CREATE and CREATE2 EIP-1167 clones with atomic initialization. Small complete examples Owned counter Deploy with an owner constructor argument. increment succeeds only from that address, and the generated count getter returns the current value. Typed token payout The interface supplies the selector and ABI schema. The external call can reenter, so the production version must account for any state it adds before making the call. Compile an example Example quality policy Shipping examples should be: - small enough to explain one concept clearly; - compiled in CI; - paired with runtime assertions when behavior matters; - explicit about security assumptions; - free of audit-only names such as repro or adv in the public learning path; - linked from a guide page that explains expected output. The repository also contains adversarial and generated examples used for compiler coverage. They are valuable engineering fixtures but are not tutorials. Repository examples still planned - ERC-20-like token with branded units and custom errors; - access-controlled vault using checks-effects-interactions; - typed interface call and try / catch ; - internal and external library linking; - upgradeable proxy deployment and safe upgrade walkthrough; - diamond deployment and selector management; - calldata slicing plus ABI decoding; - signed-message recovery with replay protection; - fixed-point math with explicit rounding once the audited math package exists.","path":"guides/examples/"},{"title":"Source units and imports","group":"Language","text":"Source units and imports A JETH source unit is a UTF-8 text file, conventionally ending in .jeth . JETH uses the TypeScript parser to obtain a syntax tree, then validates a strict on-chain subset. Being valid TypeScript syntax does not make a construct valid JETH. Top-level declarations A source unit can contain: - imports and exports; - struct-like type aliases; - branded value-type aliases; - event and error type aliases; - enums; - interfaces; - abstract and deployable classes; - static classes used as libraries. Imports JETH supports named imports from other JETH source units. Imports are resolved by the compiler's source bundle rather than by Node.js at runtime. Imported declarations participate in type checking, inheritance, interface conformance, library resolution, and selector collision checks. Use aliases when two modules export the same source name. Duplicate declarations that remain ambiguous are compile-time errors. The current command-line interface accepts one entry file. The compiler API can receive a source map for multi-file builds. A public project configuration and filesystem import resolver are part of the CLI roadmap. Exports Use export on declarations intended for another source unit: An imported contract or abstract class is not automatically deployed. Artifact production follows deployable leaf classes and library routes in the complete compilation unit. Comments Line and block comments use TypeScript syntax: NatSpec-compatible artifact documentation is not yet a stable JETH output. Do not assume comments are currently included in ABI or metadata artifacts. Native syntax only JETH previously used structural decorators such as @contract , @state , and @external . Those forms are removed. Modern JETH uses native declarations: The decorators that remain are decorators with real language meaning and no native TypeScript spelling, including @virtual , @override , @modifier , user modifier applications, @using , @nonReentrant , and proxy/diamond declarations. File and declaration names The compiler tracks declarations by source name and rejects duplicate or ambiguous declarations. Reserved language, EVM, and compiler names cannot be used as declarations where they would conflict with syntax or generated behavior. One source unit can emit multiple contract and library artifacts. Consumers should select artifacts by name, not assume the file name is the only contract name. Permanently excluded source behavior JETH does not provide runtime JavaScript or TypeScript. Constructs such as number , any , async/await, promises, generators, closures, regular expressions, eval , prototypes, typeof , instanceof , and implicit coercion have no JETH runtime meaning and are rejected.","path":"language/source-units/"},{"title":"Contract structure","group":"Language","text":"Structure of a contract Contracts use class syntax. A deployable leaf class becomes an EVM contract artifact. A contract can contain state variables, constants, immutables, functions, constructors, modifiers, events, errors, and inherited members. State variables Bare fields are persistent storage: The storage planner assigns Solidity-compatible slots and packed byte offsets. Field order is therefore part of an upgradeable contract's storage interface. An initializer executes during contract creation: Private state variables A leading makes a field private to its declaring class: Private state still occupies an ordinary storage position according to declaration and inheritance order. Privacy is compile-time access control, not storage secrecy. Anyone can inspect contract storage off chain. A derived class cannot directly read or write a base class's private field. A base implementation inherited by that derived class can still access the field. Base and derived classes may each declare the same name ; the declarations are distinct storage variables. A private field cannot use Visible<T because private storage and an external getter conflict. Public getters Wrap a state type in Visible<T to generate an external getter: Getter parameters and return values follow Solidity's public-state-getter rules for the supported storage shape. The underlying field remains available to contract code. Visible<T can also wrap a supported initialized constant or immutable field, producing an external read-only getter without allocating a storage slot. Constants An initialized static field is a compile-time constant. It consumes no storage slot and is folded at each use site. Constants must have compiler-foldable initializers. Constant expressions use exact rational semantics before conversion to the target integer type. Immutables A static value field without an initializer is an immutable. Assign it in the constructor. It is baked into runtime bytecode and consumes no storage slot. Only supported value types can be immutable. An immutable cannot be reassigned after construction. Functions Return wrappers define ABI exposure: - No wrapper means internal. - A leading means private to the declaring class. - static declares a method pure and removes the instance receiver. - External<T means externally callable and nonpayable. - Payable<T means externally callable and payable. - A read-only value-returning class method uses get ; its pure or view mutability is inferred from transitive effects. Events and errors Events and errors can be declared at file scope or as members: Use emit for events and revert or require for errors. Constructor The constructor runs once in creation code. It can accept ABI-encoded arguments, initialize state and immutables, call supported internal functions, and invoke a base constructor. A constructor is nonpayable unless decorated with @payable . Special entries A method named receive handles empty calldata and accepts value. A method named fallback handles selectors not matched by ordinary entries. Their accepted parameters and return behavior follow the JETH special-entry rules. receive is payable by definition. A plain fallback : void is nonpayable; use fallback : Payable<void when the fallback must accept and read value. The supported fallback forms are: The External<T wrapper is optional and redundant on a nonpayable fallback. A data-passing fallback receives the complete calldata and returns raw bytes. There can be at most one receive and one fallback . receive takes no parameters, returns no value, and must not use Payable<T because it is already payable by definition. Abstract contracts, interfaces, and libraries These declaration kinds are covered in their dedicated chapters.","path":"language/contract-structure/"},{"title":"Types","group":"Language","text":"Types JETH is statically typed. Every value has a type known during compilation, and the type determines its valid operations, storage representation, ABI encoding, conversion rules, and default value. This section is split into three detailed chapters: 1. Value types language/value-types.md covers integers, booleans, addresses, fixed bytes, enums, brands, and function references. 2. Reference and composite types language/reference-types.md covers dynamic bytes/string, arrays, mappings, structs, nested composites, and tuples. 3. Data locations and copying language/data-locations.md covers storage, calldata, memory, aliasing, deep copies, dynamic cleanup, and delete . Type families Family JETH syntax Main representation --- --- --- Unsigned integer u8 through u256 EVM word, checked arithmetic Signed integer i8 through i256 Two's-complement EVM word Boolean bool Strict zero/one ABI word Address address 160-bit value Fixed bytes bytes1 through bytes32 Left-aligned byte sequence Dynamic bytes bytes Dynamic ABI/storage/memory value String string UTF-8 byte sequence Enum enum Name { ... } uint8 -backed nominal type Brand type X = Brand<Base Erased nominal value type Dynamic array T Location-dependent reference value Fixed array Arr<T, N Inline or aggregate value Mapping mapping<K, V Storage-only hashed association Struct type P = { ... } Ordered fields/ABI tuple Tuple A, B Multiple values Function reference inferred/supported signature Internal callable reference No implicit dynamic typing There is no any , runtime union, implicit number/string conversion, truthiness, undefined , or null . Conversions must be permitted by the source and target types and are often explicit. Worked type example This contract combines a nominal identifier, a struct, a dynamic array, a fixed-size array, and a storage mapping: UserId is represented as its u256 base in the ABI and EVM, but it remains a distinct source type. Profile is a tuple in the ABI. Its scores field makes the complete struct dynamically encoded. owners is storage-only and therefore cannot be an external parameter or return value. Shape and location Two values with the same source type can use different runtime representations in storage, calldata, and memory. Compiler support is therefore sometimes consumer-specific. For example, returning a nested aggregate and copying it to storage are different operations with different safety requirements. Continue with value types language/value-types.md .","path":"language/types/"},{"title":"Value types","group":"Language","text":"Value types Value types fit in one EVM word and are copied when assigned or passed. They do not have an explicit data-location annotation. Unsigned integers Unsigned integer types are written u8 , u16 , and so on through u256 , in multiples of eight bits. The range of uN is 0 through 2^N - 1 . Arithmetic is checked by default. An overflow or underflow reverts with Panic 0x11 . Signed integers Signed integer types are written i8 through i256 , also in multiples of eight bits. They use two's-complement EVM representation. The range of iN is -2^ N-1 through 2^ N-1 - 1 . Division of the minimum signed value by -1 overflows in checked mode. Integer literals Integer literals use TypeScript BigInt spelling: Plain JavaScript number literals such as 1 are rejected. Constants are folded with exact rational behavior, so intermediate constant division is not prematurely truncated. Numeric separators are allowed only in valid literal positions. Address-shaped hex literals are subject to EIP-55 rules. Boolean bool has values true and false . Conditions require a boolean; integers do not coerce to booleans. ABI decoding rejects dirty boolean words rather than treating arbitrary nonzero values as true. Address address represents a 160-bit EVM address. Address equality and ordering are unsigned. Arithmetic on addresses is rejected. Conversions between address and u160 are explicit. bytes20 conversions use the Solidity-compatible alignment rule. payable addr marks an address as payable for the applicable call path. Payable and nonpayable address forms use the same ABI word. Address members include supported forms of .balance , .code , .codehash , low-level calls, typed interface conversion, and contract checks. Fixed-size bytes bytes1 through bytes32 hold fixed byte sequences. ABI and storage values are left-aligned within the 32-byte word. Fixed bytes support equality, ordering, bitwise operations, shifts, supported casts, and byte indexing where applicable. Enums Enums use a one-byte uint8 representation and ABI type. Members are numbered from zero in declaration order. Explicit conversion from an out-of-range integer reverts with Panic 0x21 . An enum can have at most the representable supported member count. Branded value types Brand<Base creates a nominal value type over a value type: Brands are erased for storage, ABI, selectors, and code generation. Their purpose is compile-time separation. A TokenId , a Wei , and a plain u256 are not implicitly interchangeable. Operations over two values of the same numeric brand preserve that brand where defined. A literal can be typed in the context of its branded operand. Function references An internal function-reference type uses arrow syntax: Take a reference from an unambiguous internal or private method and call it with the declared signature: Supported references can be stored in locals, state fields, arrays, fixed arrays, and supported structs. They can be passed and returned through internal functions, selected with a conditional expression, compared with == and != , and called directly from a state field. The zero/default function reference is not callable. Invoking it reverts with Panic 0x51 , matching an uninitialized Solidity internal function pointer. Function references are internal compiler values. They are not external ABI function pointers or stable code addresses. They cannot be ABI encoded, exposed as external parameters or returns, emitted in events, converted to integers, or taken from an externally exposed or ambiguous overloaded method. Default values Newly declared values have the zero value for their type: - integers and fixed bytes: zero; - bool: false; - address: address 0n ; - enum: its first member; - branded value: the branded zero of its base type. There is no undefined or null runtime value.","path":"language/value-types/"},{"title":"Reference and composite types","group":"Language","text":"Reference and composite types Reference values can represent more than one EVM word or refer to storage, memory, or calldata. Their behavior depends on shape and location. Dynamic bytes and strings bytes is a dynamic byte sequence. string is an ABI string represented by its UTF-8 bytes. JETH does not provide JavaScript string objects. Supported operations include assignment, return, .length , indexing for bytes, storage push / pop where applicable, ABI encoding/decoding, hashing, calldata slicing, and concatenation. Storage uses Solidity's short/long representation. Values shorter than 32 bytes are stored in the header slot. Longer values use data slots beginning at the keccak hash of the header slot. Calldata slices Use .slice start, end for a zero-copy calldata sub-view. Either bound can use the supported omitted form: Slicing corresponds to Solidity's data start:end , which does not fit JETH's TypeScript-shaped parser. The bounds must satisfy start <= end <= length ; an invalid range reverts with empty data. Slices can be returned, hashed, encoded, decoded, sliced again, or bound to a local calldata view. Only calldata bytes or string values are sliceable. This includes parameters, msg.data , calldata struct fields, and calldata array elements. Storage and memory values do not gain JavaScript-style .slice behavior. Dynamic arrays T is a dynamic array. Supported operations include .length , index reads/writes, push , zero-value push , pop , whole-value return/copy on supported paths, and nested access. Create a zero-initialized memory array with the JETH constructor form: The Solidity spelling new T length does not fit the TypeScript parser and is not JETH syntax. An out-of-bounds index reverts with Panic 0x32 . Popping an empty storage array reverts with Panic 0x31 . Excessive memory allocation reverts with Panic 0x41 . Fixed arrays Arr<T, N is a fixed-length array and corresponds to Solidity T N . .length is the compile-time constant N . Runtime indexing is bounds-checked. A constant out-of-bounds index is a compile-time error. Fixed arrays can contain value or reference elements where the relevant location and consumer path is supported. Deep aggregate acceptance is intentionally shape-sensitive; check the known-limitations chapter for cleanly rejected paths. Mappings mapping<K, V is a storage-only key/value structure. Mapping entries do not store keys or a length. A value slot is derived by hashing the canonical key word with the mapping base slot, recursively for nested mappings. Supported key families include integers, booleans, addresses, fixed bytes, and dynamic bytes/string keys on their documented paths. Values can be value types, arrays, strings/bytes, structs, and nested mappings where supported. A missing key reads as the zero value. A whole mapping cannot be copied, returned, deleted, or enumerated. Structs Object-shaped type aliases declare structs: Struct fields follow declaration order. Storage fields are packed according to Solidity rules. ABI structs are tuples. Struct values can be constructed positionally. Supported contexts also permit object literals and object spread: A struct containing a mapping is storage-only. It cannot be constructed as a memory value, passed through the ABI, or returned. Nested composites Composite types can be nested: JETH's ABI codec recursively handles supported nested arrays, fixed arrays, structs, bytes, and strings. Storage access uses a unified path of fields, indices, and mapping keys. Not every location-to-location copy of every nested shape is enabled. A compiler diagnostic on such a shape is a deliberate safety gate, not permission to bypass the type system. Tuples Tuple syntax is used for multiple returns and destructuring: Omitted destructuring positions discard their component: The complete right side is evaluated before tuple assignment writes begin.","path":"language/reference-types/"},{"title":"Data locations and copying","group":"Language","text":"Data locations and copying JETH normally infers locations from context instead of spelling Solidity's storage , memory , and calldata keywords on each declaration. Location is still fundamental to semantics. Storage Contract fields and values reached from them live in persistent storage. Storage references are resolved through fields, fixed/dynamic indices, and mapping keys. Writes must preserve packing and clear obsolete dynamic tails when Solidity would clear them. Calldata External function parameters are decoded from calldata. Value parameters are validated as words. Aggregate parameters are represented as bounds-checked views or copied images depending on the consuming operation. Calldata is immutable. Dynamic offsets and lengths are checked before use. Malformed calldata normally reverts with empty data on the matching decode path. msg.data is a calldata bytes view over the complete call data, including the four-byte selector. Memory Locals that hold dynamic bytes, strings, arrays, or struct/fixed-array images use memory. Memory exists only for the current EVM call. Memory reference assignment can alias rather than deep-copy when Solidity memory assignment aliases. Mutating through one alias is then visible through the other. Copy versus alias The source and destination determine whether an operation copies data or shares a reference: - value types always copy; - calldata to memory copies and validates the consumed shape; - storage to memory copies; - memory-to-memory reference assignment can alias; - storage-to-storage whole values copy supported fields and clear overwritten dynamic data; - memory/calldata to storage performs a deep copy only for supported shapes; - mapping members are never copied as aggregate values. This distinction is a major compiler-safety boundary. JETH rejects a path when lowering it as a copy would lose aliasing that Solidity preserves. Assignment examples Dynamic storage overwrite Replacing a storage string, bytes value, array, or dynamic struct must clear data that is no longer reachable. For example, overwriting a long string with a short one clears its old hashed data slots. JETH differential tests compare those raw slots, not only the returned value. delete delete target resets a supported location to its type's zero value: Packed values clear only their lane. Struct deletion recursively clears non-mapping fields. Dynamic arrays set length to zero and clear their elements. Dynamic bytes/string clear their header and long data. Deleting a whole mapping is rejected because mappings cannot be enumerated or cleared as a whole. Deleting a mapping value is supported. Location-sensitive limitations Some aggregate operations are accepted in one location and rejected in another. For example, a storage-to-storage copy may be safe while a calldata-struct to storage copy of the same nested shape is gated because it needs a different codec. Treat the exact diagnostic as authoritative and consult the supported matrix for the shape.","path":"language/data-locations/"},{"title":"Expressions and operators","group":"Language","text":"Expressions and operators JETH expressions are statically typed and lower to EVM/Yul operations. They do not use JavaScript coercion or floating-point semantics. Arithmetic Supported arithmetic operators include: Arithmetic is checked by default. Overflow and underflow revert with Panic 0x11 . Division or modulo by zero reverts with Panic 0x12 . Use unchecked for intentional wrapping: The operand and result types determine the available operation. Addresses, booleans, bytes, and unrelated brands do not silently become integers. Constant expressions Compile-time arithmetic is evaluated exactly before conversion to the target integer type. This matches Solidity constant rational behavior. A non-integral final result, division by zero, or value outside the target range is a compile-time error. Comparisons Comparisons require compatible types. Equality is EVM value equality, with no JavaScript coercion. Address ordering is unsigned. Aggregate equality is not a general deep-equality operation. Boolean operators && and short-circuit. The right side is not evaluated when the left side determines the result. Bitwise operators and shifts Bitwise operations apply to compatible integer or fixed-bytes types. Shift results keep the left operand's type. Signed right shift follows EVM arithmetic shift behavior. Assignment Assignment expressions yield the assigned value where supported. Compound assignment reads, computes, checks, and writes the target. Prefix and postfix ++ / -- preserve their usual before/after value distinction. Both forms use checked arithmetic outside unchecked . Tuple destructuring and assignment Destructure a multi-value result or tuple literal into new locals: Assign existing value lvalues and skip unwanted components: The entire right side is evaluated before any left-side store begins. This makes swaps deterministic and prevents an early assignment from changing a later right-side component. Destructuring currently supports the documented value component paths; it is not JavaScript iterable destructuring. Literals and construction Array literals are contextually typed: Structs support positional construction and named object construction: new Array<T length allocates a supported dynamic memory array. JETH does not use Solidity's new T length spelling. Element and nested shapes remain subject to the documented memory-layout gates. Conditional expression Only the selected branch executes. Both branches must have compatible types. Ternaries over many aggregate locations are supported, but dynamic storage aggregate ternaries remain location-sensitive. Member and index access Runtime array/bytes indices are bounds-checked. Mapping access derives a storage slot rather than performing an enumeration lookup. Calls JETH distinguishes: - internal calls to ordinary methods; - external self-calls; - typed interface calls; - qualified and attached library calls; - low-level address calls; - compiler builtins. Each call family has its own mutability, ABI, evaluation-order, and failure rules. See the functions and external-calls chapters. Evaluation order JETH matches its Solidity target for observable evaluation order: - binary operands evaluate right-to-left; - call, event, error, array-literal, and return-tuple arguments evaluate left-to-right; - short-circuit and conditional expressions evaluate only the selected path; - tuple assignment evaluates the full right side before stores begin. Avoid writing code whose correctness depends on obscure operand side effects. Explicit conversions Conversions use type-call syntax: Conversions are restricted by source/target kind and width. Explicit narrowing does not authorize an unsupported cross-kind conversion.","path":"language/expressions/"},{"title":"Statements and control flow","group":"Language","text":"Statements and control flow JETH has structured control flow with EVM-compatible return and revert behavior. Blocks and scope Braces create a lexical scope: A nested declaration can shadow an outer name. Redeclaring a name in the same scope is an error. The compiler assigns unique internal names so source shadowing does not become Yul shadowing. Conditional statements Conditions must be bool . There is no integer truthiness. while and do...while while checks before the body. do...while executes the body at least once. Classic for The initializer, condition, and update use their usual structured order. A missing condition is treated as true where the syntax is accepted. for...of for...of iterates supported storage, calldata, memory, or fixed arrays: The desugaring re-reads array length and the current element on each iteration. Mutation of a storage array during iteration therefore affects subsequent iterations as it would in the equivalent indexed loop. for...in is rejected. break and continue break exits the nearest loop. continue starts its next iteration. Labeled break and continue are not part of JETH. switch JETH adds a structured switch that Solidity source does not have: The discriminant is evaluated once. A non-empty case cannot fall through implicitly. An empty case label can share the next case body. Enum switches without default must cover every enum member. Duplicate constant cases and non-terminating accidental fallthrough are compile-time errors. Return Return type and arity are checked. A function that reaches its end returns its type's zero value where Solidity does. Modifier post-code still executes around an early function-body return according to modifier nesting. Revert and require An empty revert has no data. A string reason uses Error string . A custom error uses its four-byte selector and ABI arguments. assert condition represents an internal invariant and reverts with Panic 0x01 when false. try and catch Supported external calls can be handled with try / catch . Catch clauses can distinguish ordinary error strings, panic codes, and raw failure data according to the supported syntax. Use raw data handling when the callee is untrusted or can return nonstandard revert data. delete delete resets a supported local or storage location to its zero value. Its behavior depends on storage packing and aggregate shape. See data locations and storage layout.","path":"language/control-flow/"},{"title":"Functions","group":"Language","text":"Functions Functions are declared as class methods. Their name, return wrapper, get or static modifier, body, and decorators determine visibility, ABI exposure, mutability, payability, inheritance behavior, and modifier execution. Function forms at a glance JETH declaration Visibility Mutability rule ABI entry --- --- --- --- f : T internal inferred no f : T private inferred no get f : T internal inferred read-only no get f : T private inferred read-only no f : External<T external state-mutating or void check yes get f : External<T external inferred pure or view yes f : Payable<T external payable and state-mutating yes static f : T internal declared pure no static get f : External<T external declared pure yes External<T and Payable<T are source-level return markers. They are not nested runtime values. For example, an External<u256 method returns one u256 in the ABI. Internal functions A plain method is internal. It is not emitted in the ABI: Call an internal method by bare name or through this : Internal calls are jumps inside the current EVM execution. They preserve the current msg.sender , msg.value , storage context, and revert context. They are not message calls and cannot be wrapped in try / catch . Internal functions support direct recursion and mutual recursion. The compiler computes transitive reachability and effects, including calls through supported function references. Private functions A leading is JETH's native spelling of Solidity private : A private function: - is callable only from the class that declares it; - is absent from the ABI; - cannot be accessed directly by a derived class; - can still be used by an inherited base implementation executing on a derived contract; - may have the same source name as a private function in a base class because the two declarations are distinct; - lowers like an internal function after compile-time visibility enforcement. Private functions must be called with this. name ... . The is part of the source name and cannot be replaced with a string, computed property, or the compiler's internal mangled name. A private function cannot return External<T or Payable<T . Private and ABI exposure are contradictory, so the compiler rejects the declaration rather than publishing an obfuscated selector. Read-only private helpers can use get : get current is still private. get controls the read-only axis; controls the visibility axis. Static functions A static method has no instance receiver and is declared pure. Call it through the class name: A static method cannot read instance storage, this , environment-dependent values, or immutables. It can read compile-time constants and call other pure static methods. A static method that violates its declared-pure contract is a compile-time error. static helper ... is a private static helper and is called as ClassName. helper ... . A static payable method is rejected because a static method has no instance context in which to account for received value. External functions External<T exposes a nonpayable ABI entry: An externally exposed function is a message-call boundary. Calling it by bare name is rejected. Put reusable logic in an internal helper, or use an explicit external self-call when new message-call semantics are intended. An External<T method that returns a non-void value and is read-only must use get . This prevents an ABI function from silently claiming nonpayable state-mutating behavior while its body is actually pure or view : A read-only External<void check is allowed without get because it exposes no value-returning accessor: Read-only functions and inference get declares that a class method is read-only. Despite its TypeScript-shaped spelling, a JETH get method can have parameters: The analyzer computes transitive effects: - no state, immutable, environment, or external reads: pure ; - state, immutable, environment, or read-only external access: view ; - storage writes, transient writes, logs, value transfer, or mutating calls: state-mutating. The inferred mutability is emitted in the ABI. A get body that writes state, emits an event, transfers value, or reaches another mutating operation is a compile-time error. View<T and Pure<T are interface-only markers. A class method has a body, so its mutability is inferred. Use get f : External<T for an exposed read-only implementation. Payable functions Payable<T exposes an ABI entry that accepts ETH: msg.value is available only where payability permits it. Nonpayable entries reject nonzero call value before entering the function body. A get method cannot be payable because read-only and value-receiving declarations conflict. Parameters Every parameter has an explicit JETH type: External reference-type parameters are decoded from calldata. Internal reference-type parameters follow the accepted memory, storage, or aliasing path for their shape. See Data locations and copying data-locations.md . Parameter names participate in named-argument calls and diagnostics. Duplicate parameter names, reserved names, missing types, and unsupported ABI types are compile-time errors. Return values The declared return type is checked on every reachable return path: A void function can use return; . A non-void function must return a compatible value on every reachable path. Multi-value returns use tuple syntax and ABI encode each component in order for external functions. Default arguments Trailing internal parameters can have compile-time defaults: Defaults are evaluated in declaration context and must satisfy the supported constant-expression rules. They are checked even if every current call supplies the argument. External callers still provide the complete ABI parameter list. Named arguments Internal calls can bind arguments by parameter name: Names must match parameters, cannot be duplicated, and cannot omit a required parameter. The compiler reorders the arguments to declaration order before lowering the call. Overloading Functions can be overloaded when parameter signatures distinguish them: Return type alone does not create a distinct overload. External overloads must also have distinct canonical ABI selectors. The compiler rejects ambiguous call resolution and selector collisions. Generics Internal functions can be generic over supported type parameters: Generics are monomorphized at compile time. Each concrete instantiation is type checked and lowered as specialized internal code. Generic functions cannot form a dynamic runtime type system or a generic external ABI. Internal function references Use arrow-type syntax for an internal function reference: Supported function references can be stored in locals, state fields, arrays, and supported structs; passed and returned internally; compared with == and != ; and invoked through a matching signature. An unset reference reverts with Panic 0x51 when called. Function references are not ABI-encodable external function pointers. They cannot be external parameters or returns, event values, encoded ABI values, or cast to integers. Taking the address of an exposed or ambiguous overloaded method is rejected. Calls through a reference conservatively include the effects of compatible targets when the compiler infers mutability. Function selectors An unambiguous exposed function reference has a canonical bytes4 selector: Selector lookup on an overloaded name is ambiguous unless the source context identifies one signature. The compiler also rejects distinct ABI declarations whose canonical signatures collide to the same four bytes. Function decorators Function decorators add compile-time behavior: The supported families are: - @virtual and @override for inheritance; - declared and applied user modifiers; - @nonReentrant for the transient-storage guard; - advanced proxy, diamond, and library decorators where documented. Retired visibility and mutability decorators such as @external , @public , @internal , @private , @view , and @pure are rejected. Use native return markers, , get , and static instead. Decorators are validated at declaration and application sites. An unused modifier or generic template is still type-checked through the compiler's standalone validation routes. Calls and effects Effects propagate through internal calls, private calls, function references, modifiers, and static methods. A pure caller cannot hide a storage read inside a helper. A modifier's pre-code and post-code contribute to the wrapped function's inferred mutability. External calls are separate EVM calls and can reenter. See Interfaces and external calls interfaces-and-calls.md and Security considerations ../security/considerations.md .","path":"language/functions/"},{"title":"Units, globals, and builtins","group":"Language","text":"Units, globals, and builtins JETH exposes EVM execution context and compiler-provided functions. Access affects inferred mutability. Value and time units JETH does not currently expose Solidity-style numeric suffix expressions such as 1 ether or 2 days through its TypeScript-shaped syntax. Represent values in explicit base units or use reviewed named constants and branded types: Names such as wei , ether , and time-unit keywords remain reserved so ordinary declarations cannot create misleading alternatives. Message globals Expression Type Meaning --- --- --- msg.sender address Immediate caller msg.value u256 Wei sent with the call msg.sig bytes4 First four calldata bytes msg.data bytes Complete calldata view msg.value is only available in payable contexts. msg.data includes the selector and supports length, indexing, supported slicing, copying, hashing, and decoding. Transaction globals Expression Type Meaning --- --- --- tx.origin address Original transaction sender Do not use tx.origin for authorization. A malicious intermediary can preserve the origin while changing the immediate caller. Block globals Supported block data includes: block.difficulty maps to post-merge prevrandao semantics on the configured EVM target. Block producers can influence some fields within protocol constraints; do not treat timestamp or prevrandao as an unbiasable randomness source. Address and code globals Code length is not a reliable proof that an address is permanently an EOA. Contracts under construction have no deployed runtime code, and account code can change under protocol/account behavior. Contract creation and clone builtins JETH exposes structured EIP-1167 deployment rather than arbitrary user-level CREATE or CREATE2: The four deployment helpers create state and are not allowed in read-only methods. Prediction and argument reads inspect the EVM environment. They are allowed in get methods, but not in static methods. See Contract creation and clones ../advanced/contract-creation-and-clones.md for address formulas, exact runtime layout, initialization, collisions, and failure behavior. Gas and block history These expose the corresponding EVM operations and protocol limitations. Hash functions Inputs must have a supported dynamic bytes representation. Convert or ABI-pack other values explicitly. Modular arithmetic A zero modulus follows the compiler's Solidity-compatible panic behavior rather than silently relying on the raw opcode's zero result. ABI helpers Standard encoding uses ABI head/tail layout. Packed encoding concatenates the packed representations and can be ambiguous when more than one dynamic value is included. Do not use ambiguous packed encodings for signatures or collision- sensitive hashing. Signatures and crypto Raw ecrecover matches the EVM/Solidity precompile behavior and can return the zero address on failure. The safe recovery helpers enforce the documented signature-length, low-s, and nonzero-signer rules. Signature verification must also include domain separation, chain/application context, nonce or replay protection, expiry where applicable, and the intended signer semantics. Other precompiles JETH exposes typed helpers for supported modular exponentiation, bn256, pairing, BLAKE2f, and point-evaluation paths. Each helper has strict input and failure rules. Consult the feature matrix and tests before using a niche precompile in production. Type information type T .min and type T .max expose integer bounds where supported. Function .selector exposes the canonical selector of an unambiguous ABI function. Concatenation and templates Supported string.concat and bytes.concat forms concatenate dynamic values. Template literals are accepted only as documented string-concatenation sugar; they are not JavaScript template evaluation.","path":"language/globals-and-builtins/"},{"title":"JETH-specific features","group":"Language","text":"JETH-specific language features JETH deliberately adds a small set of compile-time features beyond Solidity source syntax. They lower to audited EVM-compatible behavior. Mutability inference Class methods do not spell pure or view . The analyzer derives the strongest valid mutability from transitive effects before ABI generation. Minimal visibility External<T and Payable<T expose ABI entries. Everything else is internal unless it is a Visible<T state getter. Private members use . This keeps the internal-call versus message-call boundary explicit. Branded newtypes Brands prevent accidental mixing while erasing to their base ABI/storage type. Struct spread Spread constructs a new supported struct value and overrides named fields. It is not a general JavaScript object spread operation. for...of This is checked indexed iteration over supported array locations. Defaults and named arguments These are compile-time call conveniences for internal functions. They do not change the external ABI. Exhaustive switch Enum coverage is checked and accidental non-empty fallthrough is rejected. Generics Supported internal generics are monomorphized. There is no runtime generic dispatch or generic ABI. Transient reentrancy guard @nonReentrant provides a built-in EIP-1153 mutex without consuming a persistent storage slot. Structured clone deployment JETH exposes EIP-1167 clone creation through typed clone and predictClone builtins. The structured surface provides audited CREATE and CREATE2 lowering without exposing arbitrary init code or raw delegatecall to application source. Immutable per-clone byte arguments are available through cloneArgs . See Contract creation and clones ../advanced/contract-creation-and-clones.md for the complete execution model and examples. Deep recursion and stack scheduling JETH's Yul backend can avoid some Solidity \"stack too deep\" source failures and can execute deeper internal recursion before the EVM stack ceiling that affects some solc lowering paths. This is an implementation capability, not an invitation to write gas-unbounded recursive contracts. Design rule Every JETH-specific feature must lower to explicit, testable semantics. Syntax convenience does not authorize JavaScript behavior or weaken the compiler's acceptance bar.","path":"language/jeth-features/"},{"title":"Constructors and inheritance","group":"Contracts","text":"Constructors, inheritance, and abstract contracts JETH supports contract inheritance with Solidity-compatible state ordering, constructor execution, virtual dispatch, and override checks. Base contracts Use extends for inheritance: State fields are laid out base-first according to the resolved linearization. Changing base order or fields can change an upgradeable contract's storage. Base constructor arguments Provide base arguments in the derived constructor with super args : Constant base arguments can also use the supported heritage form. A base constructor must be initialized exactly once. Ambiguous multiple-base argument routes are rejected. Base constructors execute from the most-base contract toward the final derived contract. Field initializers run in the corresponding construction sequence. Abstract contracts An abstract contract can contain implemented members and bodyless requirements: abstract on a bodyless member is the native spelling of a required virtual member. The decorator form is also supported where applicable: A concrete leaf must implement every inherited requirement. An abstract-only source unit is still fully type-checked. It emits empty creation/runtime artifacts for independent abstract leaves rather than silently ignoring invalid bodies. Virtual members Mark an implemented member @virtual when a descendant may override it: Overrides Use @override on a replacing member: The override must match the inherited signature, exposure, mutability contract, return shape, and member kind. In multiple inheritance, an override list can be required to identify all overridden bases. super calls An overriding internal function can invoke the next implementation in the linearized base order: External ABI entries are message-call boundaries. Put reusable inherited logic in internal functions when an internal super call is required. Multiple inheritance Multiple bases are supported with deterministic linearization and collision checks. The compiler rejects ambiguous fields, incompatible method signatures, duplicate selector exposure, and unresolved override requirements. Prefer small interfaces and shallow implementation inheritance. Complex linearization makes storage, modifiers, and super behavior harder to audit. Private members A leading declares a private member. It is not visible to derived contracts: A private base member remains usable by methods declared in that base, including when those methods execute as part of a derived contract. A derived method cannot directly name this. secret or this. normalize ... from the base. The same private source name may be declared independently in a base and a derived class. Those declarations do not override each other and occupy distinct storage or function identities. Private methods are not virtual and cannot be overridden. Use ordinary internal members when descendants are intended to access or override them. See Private functions functions.md private-functions and Private state variables contract-structure.md private-state-variables .","path":"contracts/inheritance/"},{"title":"Function modifiers","group":"Contracts","text":"Function modifiers Modifiers wrap a function or constructor body with reusable checks and effects. Declaration is the placeholder for the wrapped inner body. The identifier is reserved inside modifier syntax. Application Apply a modifier by name: Modifier arguments evaluate once before the modifier body uses them: Pre-code and post-code Statements before execute before the function body. Statements after execute after every normal body completion, including an early body return. If the wrapped body reverts, the whole call reverts and post-code does not commit state. Multiple modifiers Modifiers nest leftmost-outermost: The conceptual order is outer-pre , inner-pre , body, inner-post , outer-post . Conditional or repeated placeholders A modifier can execute conditionally or more than once on supported shapes. This changes whether and how often the wrapped body runs. Treat such modifiers as control-flow constructs, not only validation macros. Return behavior A body return stores the return value, finishes the body layer, then runs modifier post-code before final ABI encoding. A bare return; in a modifier exits that modifier layer only. An enclosing modifier resumes after its own . This distinction is important for nested modifiers. Constructor modifiers Modifiers can wrap constructors: Constructor modifier execution participates in base/derived construction and immutable staging rules. Effects and type checking Modifier effects contribute to the wrapped function's mutability. A modifier that writes storage cannot be hidden inside a read-only get . Declared but unapplied modifiers are still type-checked. Generic modifier bodies are validated against real reachable types and checked again at application sites. @nonReentrant JETH includes a built-in transient-storage guard: It uses EIP-1153 transient storage, consumes no persistent storage slot, and reverts on guarded re-entry. It can protect state-mutating external entries. The guard does not replace checks-effects-interactions, authorization, invariant tests, or review. All guarded functions share the compiler-defined guard domain, so calling one guarded entry from another can intentionally reject.","path":"contracts/modifiers/"},{"title":"Interfaces and external calls","group":"Contracts","text":"Interfaces and external calls Interfaces describe contract-to-contract ABI calls. Low-level address calls are available when the callee schema is unknown or raw returndata is required. Interface declarations Interface methods have no body, so mutability cannot be inferred. Use the supported interface return wrappers to state pure , view , nonpayable, or payable behavior. Interfaces can extend other interfaces. Overloads are allowed when canonical parameter signatures differ. Events and custom errors may also be declared in an interface; they do not contribute to its EIP-165 interface ID. Selectors and interface IDs Use a type-qualified member for a function selector: For a directly declared contract member, ContractName.method.selector and this.method.selector are supported. Internal methods have no ABI selector. An overloaded name is rejected when the source context does not identify exactly one signature. type I .interfaceId returns the XOR of the selectors declared directly by interface I : As in Solidity, inherited interface methods are excluded from the derived interface's own interfaceId . Query each layer when implementing an inheritance chain. type ... .interfaceId is valid for interfaces, not contract, enum, or integer types. Typed interface calls Convert an address to the interface and call a member: The compiler emits the canonical selector, ABI-encodes arguments, checks call success, and ABI-decodes the declared result. Contract existence checks follow the call/decode path's documented behavior. Build type-checked calldata without making the call using abi.encodeCall : The function reference must resolve to one unambiguous ABI signature, and the argument tuple must match that signature. Typed calls are external EVM calls. msg.sender in the callee is the calling contract, not the original transaction sender. External self-calls Calling an exposed member through this is a message call to the current contract. It changes the call frame and is not equivalent to an internal call. Use an internal helper when shared code should preserve the current frame. Low-level calls Addresses support checked low-level call forms: The exact option set depends on call versus staticcall . A static call cannot send value or perform state changes in the callee. tryCall and tryStaticcall expose raw success and returndata without automatic failure bubbling: Always handle both the success flag and returndata. A successful call can return malformed bytes. Decoding returndata Decode a successful result explicitly: Supported call options can request decoding directly: ABI decode validates offsets, lengths, and value words for the requested type. try / catch Use try / catch when the syntax and return shape are clearer than manual raw data handling. Catch clauses can inspect supported Error string , panic, and raw failure forms. Revert bubbling revertWith data reverts with the exact bytes returned by a failed call. This preserves custom errors and nonstandard revert payloads. Call security Any external call can transfer control to untrusted code. The callee can: - reenter the caller; - consume gas; - return malformed data; - return success with an unexpected semantic result; - deliberately revert with crafted bytes; - call other contracts before returning. Update critical state before interaction where possible, validate returned values, use a reentrancy guard where appropriate, and do not use tx.origin for authorization.","path":"contracts/interfaces-and-calls/"},{"title":"Libraries and using","group":"Contracts","text":"Libraries and using Libraries use static class syntax. They can contain internal inlined functions, constants, events/errors, modifiers, and supported external functions. Internal libraries Internal library functions are inlined or lowered as internal code with no deployed library address. Their behavior is compared with Solidity internal library calls. Library constants Sibling constant dependencies can use bare or qualified names, including forward references. Cyclic dependencies reject at compile time. @using Attach library functions to a receiver type: The receiver becomes the first library argument. Multiple libraries can be listed in one @using or stacked applications. Attachment is type-directed. If an attached name collides with a genuine built-in member such as array .length , the built-in wins; call the library function by its qualified name. Native self attachment A library author can make a function attachable without requiring @using on the consuming contract. Name the first parameter exactly self : The call a.min b lowers to MathLib.min a, b . A first parameter named anything other than self does not opt in. Attachment is additive and type-directed. A real field or built-in member wins over a library attachment. If two visible libraries attach the same name to the same receiver type, the call is ambiguous and rejects. The convention also works through imports and inside supported library-to-library calls. @using Library remains useful for attaching functions whose first parameter does not use the self convention, and for making attachment scope explicit. External libraries A supported ABI-exposed function in a static class compiles to a separate library artifact. Contract calls use DELEGATECALL and require link-time address patching. The compiler result includes library artifacts and link references. A deployment tool must: 1. deploy each required library; 2. verify its deployed address and bytecode; 3. patch every reference in the dependent creation bytecode; 4. deploy the linked contract; 5. preserve build metadata for verification. Because external libraries use delegatecall, their code executes in the calling contract's storage and address context. Storage-reference library parameters are restricted to supported paths. Library restrictions Libraries cannot have ordinary contract instance state or constructors. External library methods cannot be payable because delegatecall does not transfer a new call value. Library overloads, cross-library calls, attached calls, dynamic parameters, and returns are accepted only where the compiler has a verified ABI/linking path.","path":"contracts/libraries/"},{"title":"Events, errors, and panics","group":"Contracts","text":"Events, errors, and panics Events write EVM logs. Errors and panics revert execution and roll back state. Event declarations Declare an event at file scope: Or as a contract member: Emitting events A member event can also be emitted through this with named arguments: Named fields must match the declaration exactly. They are reordered to the declared ABI order before encoding. For a non-anonymous event, topic 0 is the keccak hash of the canonical event signature. Indexed value parameters occupy topics. Non-indexed parameters are ABI-encoded in the log data. Indexed dynamic bytes/string values and supported indexed aggregates are hashed according to their Solidity-compatible indexed encoding instead of being stored directly in a topic. Emitting a log is a state-changing effect and is not allowed in a pure/view function. Anonymous events Use the supported @anonymous declaration form to omit the signature topic. Anonymous events can use the EVM topic capacity for indexed arguments, but are harder to distinguish by event signature. Error declarations Revert with the error: A member error supports named construction through this : throw is accepted only as custom-error sugar: It produces the same revert payload as the corresponding revert ... . JETH does not support JavaScript throw values, new Error ... , or arbitrary exceptions. The payload begins with the first four bytes of the canonical error signature hash, followed by ABI-encoded arguments. require No reason produces empty revert data. A string reason uses Error string . Custom error arguments use the error selector and ABI encoding. Custom-error arguments follow the compiler's Solidity parity evaluation rules. Do not put state-changing or reverting expressions in error arguments. revert revertWith is useful for exact bubbling of returndata from another contract. assert and panic codes assert represents an invariant. A false assertion reverts with Panic uint256 code 0x01 . Common runtime panic codes include: Code Meaning --- --- 0x01 Failed assertion 0x11 Arithmetic overflow or underflow 0x12 Division or modulo by zero 0x21 Invalid enum conversion 0x31 Pop from an empty array 0x32 Array or bytes index out of bounds 0x41 Excessive or overflowing memory allocation Use custom errors for expected application failures and assertions for internal conditions that should be unreachable. ABI entries Events and custom errors appear in generated ABI JSON with their parameters and indexed flags. Consumers should use the compiler artifact rather than reconstruct the ABI from source text.","path":"contracts/events-and-errors/"},{"title":"Contract ABI specification","group":"Internals","text":"Contract ABI specification Built by @a16k-lab https://github.com/a16k-lab . JETH uses the Ethereum Contract ABI for externally visible calls, returns, events, and errors. The target behavior is Solidity 0.8.35 for overlapping supported types. Function selectors The first four calldata bytes are: Return types are not part of the selector. Examples of canonical mappings: JETH source type Canonical ABI type --- --- u256 uint256 i32 int32 address address bytes4 bytes4 T canonical T plus Arr<T, N canonical T plus N enum uint8 Brand<T canonical base type struct canonical tuple form An external overload must have a unique canonical parameter signature. Static and dynamic types Value types are ABI-static. Fixed arrays and structs are static only if all of their recursive elements/fields are static. Dynamic types include: - bytes ; - string ; - every dynamic array T ; - a fixed array with a dynamic element; - a struct with any recursively dynamic field. Head and tail encoding Static values are encoded in place as 32-byte words or recursively inlined static heads. A dynamic value's head word is an offset to its tail. Offsets are relative to the start of the current ABI tuple or array pointer region, not always the start of calldata. Nested containers reset their relative offset base according to ABI rules. Worked calldata example The canonical signature is submit uint256,uint256 , so the selector is 0x2f344195 . Calling submit 7, 10, 20 produces: The offset is 0x40 from the start of the argument tuple, immediately after the selector. The successful return value is 17 , encoded as one 32-byte word. Word representation - Unsigned integers and addresses are zero-extended. - Signed integers are sign-extended. - Booleans encode as zero or one. - Fixed bytes are left-aligned and right-padded. - Dynamic bytes/string tails contain length followed by right-padded data. - Arrays contain a length when dynamic, followed by elements or element offsets. - Structs encode as tuples in field declaration order. Calldata validation JETH validates accepted calldata paths: - narrow unsigned high bits must be zero; - signed values must have correct sign extension; - bool must be exactly zero or one; - address high bits must be zero; - fixed-bytes padding must match alignment; - enum values must be in range; - dynamic heads, lengths, and accessed data must stay within calldata; - runtime indices must be in bounds. Some aggregate field reads are intentionally lazy. An unread malformed field can remain unobserved where Solidity also decodes lazily, while a whole-value copy or return validates the fields it must materialize. Return encoding Return data omits a selector and contains the ABI encoding of the return tuple. A single dynamic return therefore usually begins with an offset word to its tail. The recursive encoder can source values from calldata, memory, or storage using location-specific readers. Acceptance of a source shape does not imply every location-to-location copy is enabled. Event encoding Non-anonymous events use the canonical signature hash as topic 0. Indexed static values occupy one topic. Indexed dynamic values and supported aggregates use the event indexed hash rules. Non-indexed values use ABI tuple encoding in log data. For Submitted , topic 0 is the event signature hash, topics 1 and 2 contain the indexed sender and id, and the dynamic array is encoded in the log data tuple. Error encoding Custom errors use a selector followed by ABI arguments. Error string uses selector 0x08c379a0 . Panic uint256 uses selector 0x4e487b71 . Unauthorized address uses its own four-byte selector followed by one zero-extended address word. ABI JSON The compiler emits ABI JSON from analyzed declarations. Selectors and runtime encoding use canonical types. The current known JSON presentation gap for some struct tuples is tracked separately from runtime correctness. Use compiler-produced ABI artifacts. Do not infer exact tuple component metadata from a short source-name table. Malformed calldata deviations JETH can reject a small set of adversarial, noncanonical offset/length values earlier than solc while both reject the call. Those safer-than-solc cases are documented in the supported matrix. Honest ABI-encoder output is expected to match.","path":"internals/abi/"},{"title":"Storage layout","group":"Internals","text":"Storage layout Built by @a16k-lab https://github.com/a16k-lab . JETH follows Solidity-compatible persistent storage layout for supported types. Raw-slot compatibility is part of the differential test bar. Slots and packing Storage is divided into 32-byte slots. Consecutive value fields smaller than 32 bytes can share a slot when they fit. A value that does not fit starts in the next slot. Structs and arrays apply their own boundaries according to Solidity layout rules. Writes to packed fields use read-modify-write so neighboring lanes remain unchanged. Fixed arrays Fixed arrays are inline. Value elements pack without crossing a slot. Composite elements use their recursive storage width. Runtime indexing computes the containing slot and byte offset, then masks or sign-extends the value as required. Dynamic arrays The declared slot stores array length. Element data begins at: Whole-slot elements use a stride. Packed value elements compute a slot and lane. Composite elements recursively use the element's storage layout. push increments length after validating capacity. pop clears the removed element and decrements length. Removing a dynamic element also clears its owned tail data. Mappings For a mapping at slot p , a static key's value base is derived from: Nested mappings repeat the process with the previous derived slot. Dynamic bytes/string keys use their content-based mapping-key rule. Mappings have no stored length and cannot be enumerated from storage alone. Structs Struct fields follow declaration order and pack within the struct. A mapping field occupies its own base slot but stores no data at that slot. Nested structs and arrays expand according to their recursive layout. Dynamic bytes and string For length below 32, bytes are stored in the header slot with length metadata. For longer values, the header stores encoded length and data begins at the hash of the header slot. Overwriting or deleting a long value must clear old data slots that are no longer used. Short/long transitions are tested against raw Solidity storage. Constants and immutables Compile-time constants consume no storage. Immutables are baked into deployed runtime code and also consume no storage slot. Adding a constant or immutable therefore does not shift ordinary state fields. Inheritance State layout follows the resolved base order. Base and derived fields can pack according to the Solidity layout rules. Reordering bases or inserting fields can break upgrade compatibility. Namespaced proxy and diamond storage Proxy implementation/admin/beacon slots use their specified collision-resistant constants. Diamond/facet state uses the selected reference model and namespace. @storage \"namespace\" places fields in an ERC-7201 region. Fields with the same namespace form one logical struct and pack in declaration order: The namespace string is part of the persistent layout. Changing \"app.config\" creates a different region. The compiler-internal raw namespace mode is reserved for synthesized diamond compatibility layouts and should not be used as an application storage convention. Never mix generated proxy storage, implementation state, and custom assembly slots without a complete layout review. Upgrade checklist For each compiler and contract release: 1. save the compiler version and EVM target; 2. save the storage-layout artifact; 3. diff slot, offset, width, and recursive type changes; 4. preserve field and base order; 5. test upgrades against populated state; 6. inspect raw reserved proxy/diamond slots; 7. reject incompatible changes before deployment. The current CLI's storage artifact is useful but not yet a frozen cross-version schema.","path":"internals/storage-layout/"},{"title":"Compiler, CLI, and tooling","group":"Internals","text":"Compiler, CLI, and tooling Built by @a16k-lab https://github.com/a16k-lab . The JETH compiler accepts source text, validates and analyzes it, emits Yul and ABI metadata, then asks solc to optimize and assemble bytecode. Current requirements - Node.js 22 - npm dependencies from the repository lockfile - the bundled solc JavaScript package JETH is currently a private pre-release package. Use the repository command until the public package and standalone binary are released. Command line Run through npm: Current options: Option Behavior --- --- --yul Print generated Yul --abi Print ABI JSON --bin Print creation and runtime bytecode --layout Print storage layout -o <dir Write basic artifacts to a directory -h , --help Print usage and solc version Without an output flag, the CLI prints a compilation summary. Current output files With -o build/ , the CLI writes: Hex bytecode files do not include a 0x prefix. The output schema is not yet a frozen public artifact standard. Compiler API CompileOptions supports: Field Meaning --- --- fileName Entry file name used in diagnostics and imports evmVersion solc EVM target, default cancun sources Multi-file path-to-source map for imports CompileResult contains: Field Meaning --- --- contractName Selected artifact name abi ABI items emitted by JETH creationBytecode Creation bytecode, hex without 0x runtimeBytecode Runtime bytecode, hex without 0x yul Generated Yul source storageLayout Name/type/slot/offset entries ir Typed compiler IR diagnostics Nonfatal diagnostics libraries Separately deployable external libraries when present linkReferences Library placeholder positions when present contracts All artifacts for a multi-contract source unit For a multi-contract file, singular fields describe the first deployable contract and contracts holds artifacts in document order. Multi-file API Import paths resolve against this source map. Diagnostics are remapped to the original source file and span. Diagnostics Compilation failures throw CompileError with structured diagnostics: Use formatDiagnostics for terminal output. Tooling should prefer structured fields over parsing formatted text. Pipeline The ABI is generated from JETH IR because solc only sees the generated Yul. Backend settings The current backend invokes solc in Yul mode with optimization enabled, 200 runs, Yul optimization enabled, and the selected EVM version. The default target is Cancun. These settings are part of the current compiler behavior but are not yet exposed as a complete stable CLI configuration surface. Compile cache Set JETH COMPILE CACHE=1 to enable the development Yul-to-bytecode cache: The cache key includes full Yul, contract name, EVM version, solc version, and a cache-format version. Only successful compilations are cached. A read/write failure falls back to a fresh solc compile. The normal CLI does not enable the cache automatically. Development checks Compiler changes should add a focused regression test. Solidity-overlapping behavior should be checked at deploy and runtime, not compilation alone. The full suite should also pass with shuffled test-file order. Planned production CLI A stable public toolchain still needs: - project configuration and filesystem import discovery; - multi-file CLI builds and contract selection; - standard JSON input/output; - stable machine-readable diagnostics and exit codes; - source maps and source-level traces; - deterministic artifact manifests and build-info files; - optimizer and EVM-target controls; - incremental/watch builds; - package publication and standalone binaries; - Foundry/Hardhat integration, deployment, linking, and verification; - LSP and editor tooling.","path":"internals/compiler-and-tooling/"},{"title":"Contracts and artifacts","group":"Internals","text":"Contracts and ABI Artifacts The compiler can produce: - creation bytecode; - runtime bytecode; - ABI JSON; - generated Yul; - storage layout; - multiple contract and library artifacts; - link references for external libraries. The current CLI writes basic artifact files. A stable artifact manifest, standard JSON mode, source maps, metadata, and build-info files are roadmap items. ABI exposure - Visible<T creates a public state getter where the underlying shape is supported. - External<T creates a nonpayable ABI function. - Payable<T creates a payable ABI function. - Internal functions do not appear in the ABI. - Read-only class methods use get ; the analyzer infers pure or view . Selectors use canonical Solidity ABI signatures. Branded types erase to their base types for selectors and ABI data. ABI validation Accepted calldata is decoded with Solidity-compatible bounds and dirty-bit checks. Invalid narrow integers, booleans, addresses, enums, offsets, lengths, or aggregate layouts reject according to the supported path. Dynamic return values use ABI v2 head/tail encoding. The test suite compares execution behavior with solc, including return and revert bytes. Complex source shapes are accepted only when the relevant location and consumer path has a verified codec. Storage layout JETH plans Solidity-compatible slots and packed lanes. This includes value types, structs, fixed and dynamic arrays, mappings, dynamic bytes/string storage, and supported nested combinations. Storage correctness is checked with raw-slot differential tests, not only getter results. This matters because two contracts can return the same value while laying out upgradeable state incompatibly. Before using JETH in an upgradeable system: 1. Save the storage layout artifact for every release. 2. Diff slot, offset, and type changes. 3. Use explicit namespaced storage where the architecture requires it. 4. Test upgrades against existing state. 5. Treat compiler-version changes as storage-sensitive until proven otherwise. Libraries and linking Internal library functions are inlined. External library functions compile to separate artifacts and use DELEGATECALL with link references. A deployment system must deploy libraries, patch every reference with the correct address, and preserve the compiler's artifact-to-reference mapping. Proxies and diamonds JETH includes generated patterns for clones, ERC-1967 proxies, transparent proxies, UUPS, beacons, and EIP-2535 diamonds. Generated support does not remove the application-level risks of authorization, initialization, selector clashes, storage collisions, unsafe upgrades, or compromised owners. Read the matching engineering specs and execute end-to-end upgrade tests before using these patterns. Production use also requires independent review. Compatibility contract The intended compatibility target is behavioral parity with Solidity 0.8.35 for overlapping supported programs. The public release process still needs a frozen language version, compiler compatibility policy, standard artifact schema, and documented EVM-target policy.","path":"internals/artifacts/"},{"title":"Contract creation and clones","group":"Advanced","text":"Contract creation and clones JETH supports contract creation from inside a contract through structured EIP-1167 clone builtins. A clone is a small contract that delegates every call to a fixed implementation while keeping its own address, balance, and storage. This chapter explains the EVM CREATE and CREATE2 behavior used by those builtins, deterministic addresses, initialization, immutable arguments, failure behavior, and the boundary of JETH's current deployment surface. !IMPORTANT JETH does not currently expose arbitrary new Contract ... , raw init-code deployment, or user-level CREATE and CREATE2 . The clone builtins are the supported way for one JETH contract to deploy another contract. Supported builtins JETH expression EVM operation Result Effect --- --- --- --- clone implementation CREATE clone address writes state cloneDeterministic implementation, salt CREATE2 clone address writes state cloneWithArgs implementation, args CREATE clone address writes state cloneDeterministicWithArgs implementation, salt, args CREATE2 clone address writes state predictClone implementation, salt address calculation only predicted address reads the environment predictCloneWithArgs implementation, salt, args address calculation only predicted address reads the environment cloneArgs reads the executing contract's code tail bytes reads the environment isContract account checks EXTCODESIZE bool reads the environment The deployment builtins are allowed in nonpayable and payable methods. They are rejected in read-only methods because contract creation changes EVM state. Prediction, cloneArgs , and isContract ... are allowed in a get method, but not in a static method, because they inspect the current EVM environment. The parameter types are exact: An implementation supplied as u160 or bytes20 can be converted to an address. Prefer an explicit address ... conversion at trust boundaries. Creation code and runtime code Every EVM deployment has two different bytecode values: 1. Creation code, also called init code, executes only during deployment. 2. Runtime code is returned by the creation code and stored at the new address. The plain JETH clone creation code returns this canonical 45-byte EIP-1167 runtime: The runtime copies calldata, delegates the call to the embedded implementation, and returns or reverts with the implementation's returndata. It does not contain application storage or constructor logic. The JETH clone builtins call CREATE or CREATE2 with a value of zero. They do not forward the factory call's msg.value into the new clone. Fund the clone in a later payable call if the application needs an initial balance. CREATE clones clone implementation and cloneWithArgs implementation, args use CREATE . The resulting address depends on the creating factory address and that factory's creation nonce. In conceptual form: Two successive clone implementation calls from the same factory therefore produce different addresses even though their runtime bytecode is identical. JETH has no predictClone form for CREATE because prediction would also require tracking the factory's creation nonce and every intervening creation. Use CREATE clones when the exact address is not needed before deployment and the factory can return or emit the address after creation. CREATE2 clones cloneDeterministic ... and cloneDeterministicWithArgs ... use CREATE2 . The address is independent of the factory's creation nonce and is calculated as: Four inputs are therefore important: - the factory address; - the 32-byte salt; - the implementation address embedded in the creation code; - the immutable argument bytes, when present. Changing the factory, implementation, or immutable bytes changes the predicted address even when the same salt is reused. Ordinary initializer calldata sent after deployment is not part of the address calculation. predictClone ... and predictCloneWithArgs ... use address this as the factory address. Call prediction on the same deployed factory that will perform the deployment. Running the same method on another factory produces another address. Complete factory with atomic initialization The safest common flow is to check the implementation, deploy the clone, and initialize it in the same transaction. This removes the interval in which a third party could initialize an unclaimed clone. Compile this source as a multi-contract unit. Deploy AccountImplementation once, deploy AccountCloneFactory , then call either factory method with the implementation address. Call the returned clone through the implementation ABI, or through the CloneAccount interface from another JETH contract. The deterministic method namespaces the user salt with a caller address. This prevents unrelated callers from claiming the same caller-specific salt through that factory. The prediction method accepts the intended caller explicitly so off-chain software and other contracts can calculate the same address. The initializer has its own replay guard. Without it, anyone could call initialize again and replace the owner after deployment. What executes through a clone The clone uses DELEGATECALL , so execution combines the clone's context with the implementation's code: Property during an implementation method Value --- --- address this clone address storage reads and writes clone storage current balance clone balance msg.sender caller of the clone msg.value value sent to the clone call executing logic implementation runtime code Each clone has independent storage. Calling the implementation directly uses the implementation's own storage and is a different execution context. The implementation's constructor runs only when the implementation is deployed. It does not run for a clone. Storage field initializers and constructor storage writes affect the implementation account, not the clone. A clone starts with zeroed storage and must use a guarded initializer or derive read-only configuration from clone arguments. Implementation immutables are embedded in the implementation runtime and are therefore shared by every clone that delegates to that implementation. They are not per-clone values. Immutable clone arguments Immutable clone arguments are bytes appended to the clone's runtime code. They are useful for per-clone configuration that never changes and does not need a storage slot. The runtime layout is: The creation code for an argument-bearing clone returns both regions. The exact argument bytes contribute to the CREATE2 init-code hash, so prediction and deployment must use byte-for-byte identical encoding and argument order. cloneArgs copies the executing contract's code after byte offset 0x2d and returns that tail as bytes . It does not authenticate, validate, or assign a schema to the bytes. Decode them with the same schema used by the factory. !WARNING cloneArgs is only meaningful while the implementation is executing through a compatible clone. Calling the same getter directly on the implementation can return unrelated code bytes or fail because the implementation itself does not have the clone runtime layout. Clone arguments are public contract code, not secret data. They are immutable because deployed runtime code is immutable under the normal EVM model, not because JETH stores them in immutable state variables. Argument length also increases deployed code size. Excessive argument data can cross the target EVM's contract-code limit and make creation fail. Implementation validation The clone deployment builtins do not require the implementation address to have code. This matches the minimal clone construction model: the address is embedded in the runtime whether or not it currently contains a contract. For ordinary factories, validate before deployment: isContract ... is an instantaneous code-size check. It is false for an EOA, an unused address, and a contract while its constructor is still running. It is not proof that the code is trusted, implements the expected ABI, preserves a storage layout, or will exist forever. Pin or govern allowed implementation addresses according to the application's threat model. Deployment failure and collisions The clone helpers revert with empty revert data when the underlying CREATE or CREATE2 operation returns the zero address. Common causes include: - the deterministic address already has code or a creation nonce; - the creation code exceeds an EVM resource or code-size limit; - the factory lacks enough gas for deployment; - another protocol-level creation rule rejects the deployment. Calling cloneDeterministic twice from the same factory with the same implementation and salt attempts to use the same address, so the second call reverts. For the argument-bearing form, the argument bytes are also part of the identity. Prediction does not reserve an address. If an unrestricted public factory uses caller-provided salts, another transaction can call that factory first. Bind the salt to the intended caller or another unique namespace, enforce authorization, and initialize in the same transaction as deployment. Do not treat a predicted address as a deployed contract until code exists there. Funds sent to a counterfactual address before deployment can become inaccessible if the intended creation can no longer succeed. CREATE versus CREATE2 decision Requirement Use --- --- cheapest simple sequential factory deployment clone ... address must be known before deployment cloneDeterministic ... per-clone read-only configuration in code a WithArgs variant mutable per-clone state guarded initializer and clone storage arbitrary constructor logic or arbitrary init code not currently supported from JETH contract source CREATE2 does not make a clone upgradeable. The implementation address is fixed inside the EIP-1167 runtime. To change logic, deploy a different clone or choose an explicitly upgradeable proxy pattern. JETH and Solidity comparison Task JETH Solidity --- --- --- deploy a plain EIP-1167 clone clone implementation a clone library or assembly using CREATE deploy a deterministic clone cloneDeterministic implementation, salt a clone library or assembly using CREATE2 predict a deterministic clone predictClone ... library helper or explicit formula attach immutable clone bytes cloneWithArgs ... variants compatible clone library or custom creation code read immutable clone bytes cloneArgs code-copy helper or compatible library deploy an arbitrary contract type not supported inside JETH source new C args deploy arbitrary init code not supported inside JETH source inline assembly CREATE/CREATE2 send value during clone creation not supported by current clone builtins possible through selected library overloads or assembly The following Solidity-shaped operation is a clean JETH compile-time rejection: This restriction is a deliberate language boundary, not a statement that the EVM lacks general contract creation. JETH currently exposes the audited clone construction path and keeps arbitrary deployment gated. Production checklist - Deploy and verify the intended implementation first. - Reject a code-less implementation and enforce any allowlist or version policy. - Keep implementation storage layout compatible with the clone's expected state. - Never rely on implementation constructor writes as clone initialization. - Guard every initializer against replay. - Deploy and initialize atomically. - Namespace or authorize user-provided salts. - Use the same factory, implementation, salt, and argument bytes for prediction. - Treat immutable arguments as public and validate them before deployment. - Remember that clone creation forwards zero value. - Test collision, malformed initialization, direct implementation calls, and implementation failure paths. - Record the clone, implementation, factory, salt, argument encoding, and source version in deployment artifacts. For upgradeable routing, beacon fleets, and diamonds, continue with Proxies, beacons, and diamonds proxies-and-diamonds.md .","path":"advanced/contract-creation-and-clones/"},{"title":"Proxies, beacons, and diamonds","group":"Advanced","text":"Proxies, beacons, and diamonds JETH provides compiler-supported deployment and upgrade patterns for EIP-1167 clones, ERC-1967 proxies, transparent proxies, UUPS implementations, beacon proxies, and EIP-2535 diamonds. These features generate routing and storage machinery, but authorization, initialization, storage compatibility, and deployment ordering remain application responsibilities. !DANGER A proxy or diamond executes another contract's code in the proxy or diamond storage context. An incorrect implementation, facet, initializer, or upgrade authority can corrupt all application state. Choosing a pattern Pattern Upgrade authority lives in Upgrade scope Best fit --- --- --- --- EIP-1167 clone nowhere in the clone not upgradeable many cheap instances of one implementation Plain ERC-1967 user-written proxy method one proxy custom proxy administration Transparent generated admin branch one proxy strict admin/user call separation UUPS implementation one proxy small proxy with implementation-defined authorization Beacon shared beacon every proxy using the beacon coordinated fleet upgrades Diamond generated selector router selected selectors modular systems split across facets All patterns require deployed code at implementation, beacon, or facet addresses. JETH checks code existence on the generated paths where the corresponding reference implementation does. Minimal EIP-1167 clones Minimal clones contain a 45-byte runtime stub that delegates every call to one fixed implementation. Each clone has its own address, balance, and storage. The implementation's own storage is not used by calls through the clone. See Contract creation and clones contract-creation-and-clones.md for the full CREATE and CREATE2 model, address formulas, initialization rules, immutable argument layout, collision behavior, failure paths, and production checklist. Clone factory and implementation Deployment flow: 1. Deploy AccountImplementation once. 2. Call AccountFactory.create implementation . 3. Call initialize owner on the returned clone address, using the implementation ABI. 4. Treat the clone address as the account. Calls write the clone's storage. cloneDeterministic uses CREATE2 . predictClone returns the address for the factory address, implementation, and salt. A repeated deployment with the same inputs fails because the predicted address already contains code. Clone immutable arguments JETH can append immutable argument bytes to the clone runtime. The implementation reads its current clone's appended tail through cloneArgs : The exact immutable argument bytes contribute to the deterministic address. cloneArgs is meaningful only while code is executing through a compatible clone. Clone deployment mutates state and is rejected from a read-only method. Prediction, code inspection, and clone-argument reads are environment reads and therefore infer view , not pure . Plain ERC-1967 proxy @proxy generates a payable delegate fallback and uses the ERC-1967 implementation slot. The class may define its own administration functions but may not define ordinary state, receive , or fallback members. 0xfe4b84df is CounterV1.initialize u256 .selector . Prefer producing selector values from compiler artifacts or a typed interface instead of copying a magic constant between projects. proxyInit accepts either implementation, initData or implementation, admin, initData . It checks that the implementation contains code, stores the ERC-1967 slots, emits Upgraded , and delegatecalls nonempty initialization data. A failed initializer bubbles its revert data and the whole deployment reverts. upgradeProxy newImplementation, data checks code, updates the implementation slot, emits Upgraded , and delegatecalls nonempty migration data. JETH does not authorize this builtin for you. The surrounding exposed method must perform the required ownership, role, governance, or timelock check. Interact with the deployed proxy using CounterV1 's ABI. The proxy's own administration ABI is used only for the explicitly declared proxy methods. Transparent proxy @proxy 'transparent' generates the transparent admin branch. The source class contains only its constructor: Routing depends on the caller: - the admin may call only upgradeToAndCall address,bytes ; - any other admin call reverts with ProxyDeniedAdminAccess ; - a non-admin call always delegates to the implementation, including a call whose selector equals upgradeToAndCall address,bytes . This caller-based routing prevents the admin from accidentally invoking an implementation function through the proxy. A transparent proxy cannot declare exposed methods, state fields, receive , or fallback . Use the generated upgradeToAndCall selector from the administrator account and use the implementation ABI from every normal account. UUPS proxy UUPS keeps the proxy minimal and puts upgrade behavior in the implementation. Use a plain @proxy for routing and mark each compatible implementation with @uups . The initialize address selector is 0xc4d66de8 . A valid @uups implementation must declare exactly the internal gate authorizeUpgrade newImplementation: address : void . JETH synthesizes: - payable upgradeToAndCall address,bytes ; - read-only proxiableUUID : bytes32 . Call upgradeToAndCall at the proxy address. The proxy delegates it into the current implementation, so authorization reads proxy storage and the upgrade writes the proxy's ERC-1967 implementation slot. Before updating the slot, the generated path calls the new implementation's proxiableUUID and requires the ERC-1967 implementation-slot identifier. This rejects a target with no compatible UUPS surface or a wrong UUID. It does not prove that the new implementation preserves storage, authorization, or application invariants. Do not declare your own upgradeToAndCall or proxiableUUID on an @uups class. Do not combine @uups and @proxy on the same class. Beacon and beacon proxy A beacon stores one current implementation for a fleet of proxies. Every beacon proxy asks the beacon for implementation on each routed call. For @beacon , JETH generates: - owner : address ; - implementation : address ; - owner-gated upgradeTo address ; - Upgraded address events on construction and upgrade. The beacon owner is the beacon deployment caller. The constructor's implementation must contain code. The user-written beacon class contains only the empty constructor and cannot declare state, inheritance, special entries, or methods that collide with the generated surface. proxyInitBeacon beacon, initData stores the ERC-1967 beacon slot, emits BeaconUpgraded , resolves the current implementation, and delegatecalls nonempty initialization data. proxyBeacon reads the stored beacon address. Deployment flow: 1. Deploy the implementation. 2. Deploy AccountBeacon implementation from the intended owner account. 3. Deploy one or more AccountBeaconProxy beacon, initialValue instances. 4. Interact with each proxy through the implementation ABI. 5. Call AccountBeacon.upgradeTo newImplementation as the beacon owner. 6. Verify every dependent proxy against populated, independent state. One beacon upgrade changes routing for every proxy using that beacon. This is the defining benefit and the primary blast-radius risk. Namespaced storage for delegatecalled code Facets and other modular delegatecall targets should use collision-resistant namespaced state. JETH's @storage \"namespace\" uses the ERC-7201 location: Fields with the same namespace share one logical struct and follow declaration order and packing inside that namespace. Different namespaces are isolated. Ordinary fields still use sequential contract storage and should not be used for facet-owned state. Namespace strings are permanent storage identifiers. Renaming one creates a new storage region and makes the old values unreachable through the renamed fields. All facet versions sharing a namespace must preserve compatible field order and types. EIP-2535 diamonds @diamond generates the selector router, cut operation, loupe, ERC-165 support, ownership, initialization, and finalization surface. Facets are separately deployed @facet contracts whose exposed function selectors are installed by a cut. Diamond models Declaration Storage compatibility Ownership and fallback behavior --- --- --- @diamond or @diamond \"array\" diamond-1/diamond-3 array model direct ownership transfer; unknown selector reverts @diamond \"packed\" diamond-2 packed selectors, eight per word direct ownership transfer; unknown selector reverts @diamond \"solidstate\" Solidstate v0.0.61 namespaces two-step ownership and configurable default fallback Choose the model for compatibility with an existing deployment or toolchain. The models are not storage-compatible with each other. Do not change a deployed diamond's model. Declaring a diamond Array and packed diamonds use diamondInit owner : Solidstate uses its matching initializer: The source diamond contains only its constructor. JETH rejects user-declared fields, methods, events, errors, modifiers, inheritance, receive , and fallback because the complete surface is synthesized. A class cannot be both @diamond and @facet , and one source file cannot declare multiple diamonds. Generated diamond ABI Every model provides: Function Purpose --- --- diamondCut FacetCut ,address,bytes add, replace, or remove selectors and optionally run initialization facets return every installed facet and its selectors facetFunctionSelectors address return selectors installed for one facet facetAddresses return installed facet addresses facetAddress bytes4 resolve one selector supportsInterface bytes4 query registered interface support owner return the upgrade owner transferOwnership address transfer or nominate ownership, depending on model freezeDiamond permanently disable future cuts isFrozen report finalization state The Solidstate model additionally provides nomineeOwner , acceptOwnership , getFallbackAddress , and owner-gated setFallbackAddress address . On a selector miss, it delegates to the configured fallback address when nonzero. The array and packed models revert on a selector miss. Installing facet selectors FacetCut has this ABI shape: Action values are: Value Action Required facet address --- --- --- 0 add selectors that do not exist deployed facet address 1 replace selectors that exist new deployed facet address 2 remove selectors that exist zero address For the CounterFacet example, an add cut contains the deployed facet address and the selectors of increment , current , and writer . Submit it to diamondCut cuts, address 0n , bytes \"\" from the diamond owner. After the cut, call the diamond address with the CounterFacet ABI. The router delegatecalls the facet and the app.counter fields are stored at the diamond address. Selectors for a cut should come from compiler-produced ABIs or qualified .selector expressions. Do not derive selectors from display names or return types. Cut initialization The second and third diamondCut arguments optionally run one initialization delegatecall after the selector changes: - use address 0n and empty bytes to skip initialization; - otherwise init must contain code; - calldata is delegatecalled into init in diamond storage context; - initialization failure reverts the complete cut and bubbles revert data. An initializer is arbitrary delegatecalled code. Protect it against replay when appropriate, use namespaced storage, and include it in the storage and security review. Loupe and routing checks After every cut, verify at minimum: 1. facetAddress selector equals the intended facet; 2. facetFunctionSelectors facet contains exactly the expected set; 3. facetAddresses contains no stale address; 4. each installed selector succeeds through the diamond address; 5. removed selectors revert or reach only an intentionally configured Solidstate fallback; 6. the owner and registered interface IDs remain correct. Add, replace, and remove use model-specific storage algorithms. Their observable ordering can affect loupe output and raw storage, so an upgrade test should compare complete selector sets rather than only one successful call. Finalizing a diamond Every diamond starts upgradeable. The owner can call freezeDiamond to set a permanent flag that makes later diamondCut calls revert. Existing selector routing remains active. Finalization is irreversible through the generated surface. Before freezing, install every required selector, validate loupe output, complete initialization, test recovery procedures, and confirm there is no alternate privileged path in a facet or Solidstate fallback. Storage compatibility rules An implementation upgrade must preserve the proxy-visible layout: - do not reorder, remove, or change existing state fields; - append compatible fields after existing fields; - preserve inheritance order and packing assumptions; - preserve initializer guards and authorization state; - do not reuse ERC-1967 implementation, admin, or beacon slots. A facet upgrade must preserve every namespace it uses: - keep the exact namespace string; - keep existing field order, widths, and recursive shapes; - coordinate fields shared by multiple facets; - never use the diamond router's internal namespaces; - review default fallback code as a full-storage delegate target. Implementation constructors initialize only implementation storage. Proxy or diamond state must be initialized through a delegatecall initializer. Production deployment checklist Before deployment or upgrade: 1. pin the JETH revision, solc 0.8.35, EVM target, optimizer settings, and source hashes; 2. deploy and verify every implementation, beacon, facet, and initializer; 3. record implementation, admin, beacon, owner, and namespace expectations; 4. encode initialization from the intended ABI and test replay protection; 5. diff storage layouts against the deployed version; 6. test the upgrade with populated state, not only an empty deployment; 7. verify events, returndata, revert bubbling, and raw reserved slots; 8. verify every proxy or diamond through its delegated ABI; 9. test unauthorized, malformed, zero-address, and no-code targets; 10. test rollback or incident response before transferring production control; 11. place authority behind reviewed governance, multisig, or timelock policy; 12. obtain an independent audit of application logic and the exact generated pattern used by the selected compiler release. Generated upgrade machinery reduces boilerplate. It does not make an upgrade, initializer, storage layout, facet set, or governance process safe by itself.","path":"advanced/proxies-and-diamonds/"},{"title":"Security considerations","group":"Security","text":"Security considerations Built by @a16k-lab https://github.com/a16k-lab . This chapter covers contract-author risks. Compiler correctness is addressed in the next chapter. !DANGER Passing compiler tests does not make an application safe. Production contracts still require application-specific testing, economic review, deployment review, and an independent audit. Reentrancy External calls transfer control. Update critical state before interaction, validate invariants after return where necessary, and use @nonReentrant for an appropriate guard boundary. Do not assume a static-looking interface prevents the callee from calling other contracts. The state reduction occurs before the external call, and @nonReentrant protects the chosen entry boundary. In a real escrow, also define receiver, pause, accounting, and forced-ETH policies. Authorization Use msg.sender , role state, signatures with replay protection, or a reviewed governance mechanism. Never use tx.origin for authorization. Check authorization on every state-changing entry, including upgrade, pause, rescue, callback, and initialization paths. Constructor authorization does not protect later entries. Every privileged state transition needs its own check or a correctly applied modifier. Arithmetic and precision Checked integer arithmetic prevents silent overflow outside unchecked , but it does not prevent: - precision loss from division; - unfavorable rounding; - unit confusion; - economic overflow in an intentionally wide range; - incorrect fixed-point scaling; - division before multiplication; - malicious boundary inputs. Use branded units, explicit rounding policies, range checks, and reference vectors. Calls and returndata A low-level call success flag means the EVM call did not revert. It does not mean the intended application action occurred. Validate returndata and postconditions. Bubble raw revert data only when exposing the callee's error surface is intended. Crafted revert bytes can mimic known selectors. ABI and signatures Avoid ambiguous abi.encodePacked inputs when hashing multiple dynamic values. Include domain separation, chain/application context, signer, nonce, action, parameters, and expiry as needed. Enforce low-s signatures through the safe recovery helper and reject the zero address. Randomness and block data Timestamp, coinbase, prevrandao, blockhash, gas, and ordering can be influenced or predicted to varying degrees. Use a reviewed randomness protocol for valuable outcomes. Denial of service and gas Loops over user-growing storage can become uncallable. External calls can consume gas. Large calldata or returndata can increase copy costs. Deep recursion and large memory allocations can fail. Bound iteration, use pull patterns, paginate, and make progress resumable where appropriate. Storage and upgrades Upgradeable systems must preserve field order, packing, inheritance order, namespaces, and reserved proxy slots. Initialize every proxy exactly once and protect upgrade authorization. An implementation constructor does not initialize proxy storage. Delegatecall External libraries, proxies, and diamonds execute code in another account's storage context. A delegate target can modify any storage slot and act as the calling contract. Only delegate to verified code selected by trusted, audited logic. Error handling Use custom errors for expected failures. Use assertions for internal invariants. Do not ignore low-level call success. Do not rely on revert strings as a stable machine protocol. Compiler and dependency pinning Pin the JETH version, solc version, EVM target, dependencies, optimizer settings, and source inputs. Preserve artifacts and hashes. Recompile and re-audit when the toolchain changes. Audit scope Passing the JETH differential suite does not audit an application. Production contracts still need invariant tests, fuzzing, economic analysis, deployment review, key-management review, and an independent audit.","path":"security/considerations/"},{"title":"Compiler correctness model","group":"Security","text":"Compiler correctness model Built by @a16k-lab https://github.com/a16k-lab . JETH uses a safety-first acceptance policy because a compiler can successfully produce bytecode that is still wrong. Bug classes Miscompile MC JETH accepts source but generated behavior differs from the intended or Solidity-equivalent behavior. This includes wrong return/revert bytes, storage, logs, control flow, calls, or deployment behavior. Over-acceptance OA JETH accepts source that should be rejected by the language or parity target. An OA is dangerous because it can expose a code-generation path that was never soundly implemented. Over-rejection OR JETH rejects a valid source shape. This limits expressiveness but is safer than wrong bytecode. ORs should be lifted only with end-to-end proof. Flake A test result depends on order, time, shared state, random seed, environment, or race behavior instead of the source under test. Differential evidence For a Solidity-overlapping feature, a strong test: 1. creates equivalent JETH and Solidity sources; 2. compiles both with pinned toolchains; 3. deploys both on the same EVM configuration; 4. executes success and failure paths; 5. compares return and revert bytes; 6. compares event topics/data; 7. compares relevant raw storage slots; 8. covers boundaries and malformed calldata; 9. preserves a deterministic witness. \"Both compile\" is not sufficient evidence. Clean rejection When the analyzer cannot prove a storage copy, ABI transcode, aliasing behavior, or control-flow shape sound, it emits a diagnostic before Yul generation. Unsupported IR should not silently reach a partial lowering. Backend guards are the final defense, not a substitute for analyzer admission checks. Test order and seeds The suite runs in deterministic and shuffled file order to detect leaked compiler state. Random campaigns must record their seed and minimize failures into focused pins. Meaning of zero known miscompiles This statement means no unresolved witness exists in the current corpus at a specific revision. It does not prove the absence of unknown bugs. It must be scoped by compiler revision, solc version, EVM target, and audit coverage. AI-assisted auditing An offline model can propose programs, mutations, invariants, and bug hypotheses. The model is not the oracle. JETH, solc, a local EVM, deterministic comparisons, and human root-cause review decide whether a finding is real. Release bar A production compiler release needs: - no unresolved known MC or OA; - documented deliberate ORs; - normal and shuffled suite passes; - reproducible build artifacts; - stable diagnostics/artifact schemas; - source maps and debugging support; - independent audit and disclosure process; - an exact compatibility statement.","path":"security/compiler-correctness/"},{"title":"Syntax cheatsheet","group":"Reference","text":"Syntax cheatsheet Declarations State, constants, and immutables Functions Internal function references Locals and assignment Control flow Errors and events Calls and ABI Contract creation and clones These helpers deploy EIP-1167 clones through CREATE or CREATE2. Arbitrary new Contract ... deployment is not currently supported. See the complete creation and clone guide ../advanced/contract-creation-and-clones.md . Modifiers and inheritance Common conversions See the detailed chapters for restrictions, location rules, and runtime behavior.","path":"reference/cheatsheet/"},{"title":"Differences from TypeScript and Solidity","group":"Reference","text":"Differences from TypeScript and Solidity JETH uses TypeScript-shaped syntax but EVM semantics. It also intentionally differs from Solidity source syntax in several ergonomic areas. TypeScript syntax, not TypeScript runtime TypeScript intuition JETH behavior --- --- number and floating point Rejected 1 numeric literal Rejected; use 1n String + concatenation Rejected; use concat helpers Truthy/falsy conditions Rejected; condition must be bool Dynamic objects No runtime object model undefined or null No runtime value Exceptions with throw Use EVM revert forms async/await Rejected Closures and captured variables Rejected Prototype methods No prototype runtime Garbage collection No managed runtime JETH spelling versus Solidity Concept JETH Solidity equivalent --- --- --- Contract class C contract C Abstract contract abstract class C abstract contract C Library static class L library L Struct type P = { x: u256 } struct P { uint256 x; } Unsigned integer u256 uint256 Signed integer i128 int128 Fixed array Arr<T, N T N Mapping mapping<K, V mapping K = V External function f : External<T function f external returns T Payable function f : Payable<T function f external payable returns T Public getter x: Visible<T T public x Internal method ordinary method internal function Private member name private member Constant static K: T = value T constant K = value Immutable static K: T; T immutable K Interface view f : View<T function f external view returns T Mutability Solidity asks the author to write pure and view . JETH infers them from the body and transitive calls. Interfaces still declare mutability because they have no body. Compared example JETH: Solidity: JETH infers view from the storage read. The exposed JETH getter still uses get to declare its read-only contract. Visibility JETH exposes only methods wrapped in External<T / Payable<T and fields wrapped in Visible<T . Ordinary methods are internal. This is narrower than Solidity's several visibility keywords. JETH private members use and stay scoped to their declaring class: The Solidity equivalent spells the same visibility explicitly: JETH-only compile-time features - branded newtypes; - struct spread; - array for...of ; - default and named internal arguments; - exhaustive switch ; - monomorphized internal generics; - built-in transient @nonReentrant . Evaluation and arithmetic JETH follows its Solidity target for checked arithmetic, panic codes, short-circuiting, and observable evaluation order. Do not infer behavior from JavaScript evaluation or coercion. add 255, 1 panics with arithmetic code 0x11 . wrap 255, 1 returns zero. This is EVM integer behavior, not JavaScript number behavior. Safer malformed-calldata behavior For honest ABI-encoded inputs, supported paths target Solidity parity. JETH intentionally rejects a few malformed, noncanonical offset/length forms earlier than solc. Both implementations fail the call, but revert data can differ. Deliberate exclusions Some Solidity features or aggregate paths remain cleanly gated. Some JavaScript features are permanently excluded because they lack a sound on-chain meaning. See known limitations and the complete supported matrix.","path":"reference/differences/"},{"title":"Compiler diagnostics","group":"Reference","text":"Compiler diagnostics JETH diagnostics identify source spans and use codes such as JETH470 . Reading a diagnostic A diagnostic normally contains: - a stable-looking code; - source file and span; - a human explanation; - sometimes the supported alternative or reason for a safety gate. The public machine-readable diagnostic schema and code stability policy are not yet frozen. Tooling should not parse human prose as a permanent API. Diagnostic categories Diagnostics cover: - unsupported TypeScript syntax; - reserved or duplicate declarations; - unknown names and members; - type mismatch and invalid conversion; - visibility/mutability/payability violations; - invalid inheritance, override, interface, or library structure; - ABI, calldata, memory, or storage shape restrictions; - unsupported copy/aliasing paths; - constructor, immutable, or modifier rules; - call and return arity; - constant evaluation and range errors; - backend soundness guards. Examples An invalid implicit narrowing is rejected at the conversion site: Make the policy explicit only when the range is known and checked: An unsupported storage transcode is a safety gate, not a request to bypass the type system: The diagnostic is emitted before bytecode generation because accepting this shape without a proven transcode would risk a miscompile. Clean rejection is intentional A \"not supported\" diagnostic is preferable to accepted source that would produce wrong bytes. Do not suppress or work around such an error by changing types until the resulting semantics are understood. Filing a compiler issue Include: 1. exact JETH revision/version; 2. exact Node and solc versions; 3. minimal JETH source; 4. Solidity mirror when applicable; 5. full diagnostic or runtime bytes; 6. deploy/call inputs; 7. expected and actual return/revert/log/storage data; 8. deterministic seed for generated failures. Classify whether the report is a possible MC, OA, OR, diagnostic-quality issue, or flake. The compiler team should confirm the classification from executable evidence.","path":"reference/diagnostics/"},{"title":"Supported features and limitations","group":"Reference","text":"Supported features and known limitations JETH's language surface is intentionally evidence-driven. A source shape is accepted only when its analyzer and backend path are supported. !WARNING A type being supported in one location or operation does not imply every copy, call, return, assignment, hash, or ABI path for that type is supported. Authoritative matrix The exhaustive current list is maintained in SUPPORTED.md ../../../SUPPORTED.md . It includes supported shapes, deliberate diagnostics, safer malformed-calldata deviations, and historical parity notes. Important current gates Representative clean rejections include: - selected element access into a dynamic-array field of a calldata struct; - selected aggregate parameters/returns through internal calls; - some memory structs containing dynamic-array fields; - memory/calldata struct-to-storage copies containing a dynamic array with a fixed-inner value-array level; - selected whole fixed-array element assignments; - selected static calldata aggregates inside multi-value returns; - ternaries over dynamic storage aggregates; - selected nested packed dynamic-array paths; - remaining pointer-headed memory/decode shapes; - arbitrary new Contract ... and raw init-code CREATE/CREATE2 deployment beyond the structured EIP-1167 clone builtins; - artifact/CLI presentation gaps such as tuple component JSON and source maps. The exact diagnostic and compiler revision matter. This summary should not be used as an admission predicate. Why support is path-specific The same source type can have different runtime representations in storage, memory, and calldata. Returning a value, assigning it, hashing it, passing it to an internal function, and storing it can require different transcodes and aliasing behavior. Therefore: Workarounds Safe workarounds are explicit and local: - read or write supported leaf fields instead of an unsupported whole-value copy; - move shared logic to a supported internal helper signature; - select a dynamic storage branch before performing the aggregate operation; - use a storage source where storage-to-storage copy is supported; - encode/decode through a documented supported ABI path; - split a large shape into smaller supported fields. Never use a workaround that depends on guessed memory offsets or storage slots. Unsupported whole-value copy and safe redesign This shape is intentionally rejected because it contains a dynamic array whose element is a fixed-size value array: When changing the application schema is acceptable, flatten the pair data into a supported dynamic value array and preserve the pair invariant explicitly: This is a schema change, not a representation trick. Existing protocols should not silently substitute it for an established ABI. Permanently excluded features JavaScript runtime features without a clear EVM meaning remain rejected. This includes floating-point number , any , async/await, generators, closures, regular expressions, eval , implicit coercion, and prototype behavior. Versioning The accepted set can grow as ORs are safely lifted. It must not silently shrink or change semantics in a stable release without a documented compatibility decision.","path":"reference/known-limitations/"},{"title":"Complete feature matrix","group":"Reference","text":"JETH - supported vs unsupported features Built by @a16k-lab https://github.com/a16k-lab . Running checklist per directive §10.7. Updated through Phase 4 + the solc-parity feature sweep closed all 17 audit-found compile-time gaps; runtime byte-identical to solc , plus enums and the six distinctive features F1-F6 see the next section and docs/distinctive-features.md docs/distinctive-features.md . The full suite is 1500+ differential tests against solc-js returndata + raw storage slots + event logs , zero known miscompiles. JETH is native-syntax only. There is no decorator \"mode\". The examples throughout this file use the native surface a bare class is the contract, a bare field is state, External<T / Payable<T expose functions, mutability is inferred . The retired // use @decorators pragma and the 21 legacy structural decorators are hard compile errors - see Legacy decorator removal legacy-decorator-removal-native-syntax-only below for the full native-spelling table. Legacy decorator removal native-syntax only JETH used to accept two per-file syntaxes: a legacy decorator syntax opted into with a // use @decorators pragma and the native TypeScript-shaped syntax. The legacy syntax is now fully removed ; native is the only syntax. - A source that still carries the retired // use @decorators pragma is a hard compile error, JETH480 , reported at the directive line before any other pass parse, diamond expansion, bundling , so it is the first thing a legacy file sees. - The 21 structural / visibility / mutability decorators below were removed. Each is a hard compile error, JETH481 , whose message points at the native spelling to use instead. The scan runs on the original user source, so the compiler's own internally-synthesized decorators - e.g. a @diamond expansion - are never flagged. Removed decorator Native spelling --- --- @contract class C { ... } a bare leaf class C { ... } @abstract class C { ... } abstract class C { ... } @struct class P { ... } a type P = { a: T; ... } object-type alias @interface class I { ... } a TS interface I { m args : View<T } ; implement via class C extends I @library class L { ... } a static class L { ... } @external m : void state-mutating m args : External<T void returns External<void @external m : T read-only, value-returning get m args : External<T @external / @public field x: Visible<T public getter @public m m args : External<T Payable<T if payable @internal m drop it - a bare method/field is internal @private member a leading e.g. x , f @view / @pure / @read drop it - in a CLASS, mutability is INFERRED from the body: get m args : T view if it reads state/env, pure if it reads nothing; add External<T to expose it , or static m args : T / static get m args : T . View<T / Pure<T are INTERFACE-ONLY markers JETH498 - an interface method has no body, so it must state its per-method mutability @payable m m args : Payable<T @state x: T a bare field x: T @constant K: T = v a static K: T = v field @immutable K: T a static K: T; field no initializer; assigned in the constructor @event E ... / member E: event<{ ... } a field or file-level type E = event<{ ... } @error E ... / member E: error<{ ... } a field or file-level type E = error<{ ... } @indexed p: T p: indexed<T inside the event<{ ... } shape @receive a method named receive { ... } payable implied, no marker @fallback a method named fallback { ... } The keep-list decorators are still legal they have no native spelling : @virtual , @override and its override list , @modifier and every user-named modifier application, @nonReentrant , @using , @diamond , @storage , @proxy , @beacon , @facet , @anonymous , and @payable on a constructor . Separately, @hidden was never a native spelling and stays rejected JETH440 : internal is the bare default, so a member needs no visibility decorator at all. An abstract-only translation unit is fully type-checked and produces empty creation/runtime bytecode. Independent abstract leaves produce separate empty artifacts in CompileResult.contracts , matching solc's per-contract artifact model; an invalid body in any leaf rejects the whole unit. Differential audit 2026-06-20 - fixes A fresh adversarial differential audit against solc 0.8.35 found and fixed the following. All are verified byte-identical to solc and the suite stays green 1672 tests . Silent miscompiles fixed the dangerous class - JETH accepted + ran but produced wrong results : - Constant rational arithmetic : a compile-time-constant expression is now evaluated as an EXACT rational and collapsed to a single range-checked integer literal solc semantics , instead of eager truncating integer division. 10n/4n 4n is now 10 was 8 ; 1n/2n + 1n/2n is 1 was 0 . As a side effect, compile-time constant overflow / div-by-zero / 2n 256n / non-integer-rational results are now rejected at compile time JETH070 / JETH079 , matching solc, instead of deferring to a runtime panic or silently truncating. Variable arithmetic incl. unchecked wrapping is unchanged - only fully-constant subtrees fold. - enum declared INSIDE the contract class previously produced an EMPTY ABI every function silently dropped, all calls revert . In-class enums are now hoisted to top level pre-parse and the contract compiles normally TS cannot parse an enum as a class member . - Nested array literal returns return 1n,2n , 3n,4n directly typed as a dynamic u256 previously emitted malformed ABI inner offsets past the returndata . A dynamic array literal must have value-type OR nested VALUE-LEAF-array elements u256 , Arr<u256 ,2 , ... ; a literal with a bytes / string / struct element is rejected JETH216 . NOTE: a nested value-leaf literal bound to a typed nested MEMORY LOCAL let m: u256 = .. , .. ; return m is now fully supported byte-identical to solc see \"Nested / multi-dimensional MEMORY-array LOCALS\" below . Over-acceptances fixed JETH accepted programs solc rejects : - Conflicting state mutability a read-only function that is also payable is now rejected JETH052 . - A public state getter a Visible<T field was silently ignored no getter . It now auto-generates a getter solc parity ; a getter colliding with a same-named function is a clean JETH044 . Supported shapes, all byte-identical to solc: - value-type and bytes / string vars: name view returns T . - mappings nested ok , including value-type/ bytes / string / bytes -or- string -KEY mappings, small-value masking, and a mapping K= T value trailing uint256 index param . - value-element arrays of any nesting T , Arr<T,N , T , T , mapping K= T , mapping K= Arr<T,N : one uint256 index param per dimension, incl. packed elements; string / bytes return the element. Out-of-bounds reverts with EMPTY data matching solc's getter, NOT Panic 0x32 . - bytes / string multi-level arrays string , mapping K= Arr<string,N , ... : the dynamic element at the resolved place, empty-revert on OOB. - struct vars, and structs reached via a mapping/array mapping K= S , S , Arr<S,N , mapping K= S : flattened to a tuple - the TOP struct OMITS all array + mapping members; a kept nested struct is a FULL sub-tuple whose FIXED arrays ARE included. An all-static nested struct is inlined; a DYNAMIC nested struct a bytes / string /dynamic-array member at depth is emitted as a whole storage-struct component head/tail at any nesting depth, reached via a constant OR a runtime mapping/array slot. Byte-identical to solc incl. empty-revert on array OOB, zero-struct absent key . The auto-getter now matches solc for EVERY storage type JETH supports. The only public-getter Visible<T vars solc accepts that JETH rejects are ones whose underlying STORAGE TYPE is itself unimplemented so a manual getter or a write is rejected too, not a getter-specific over-rejection , e.g. string 3 a dynamic array of fixed arrays of a dynamic type, JETH217 . Over-rejections fixed valid Solidity JETH wrongly rejected : - for const x of this.structArray and a typed let p: S = this.arr i / this.m k storage struct-array element / mapping struct value - memory local are now supported. - A nested switch that terminates on every branch now satisfies the case-terminator analysis no spurious \"add a trailing break\" JETH284 . - EIP-55 address hex literals: a 40-hex-digit bigint literal that passes the EIP-55 checksum all- numeric literals pass trivially is now of type address , so K: address = 0x<40 hex n and x == 0x<40 hex n work WITHOUT an address ... cast byte-identical to solc . Such a literal is address-typed everywhere: it casts only to u160 / bytes20 , never implicitly to an integer. Over-acceptances fixed same EIP-55 change : a 39/41-hex-digit literal, or a 40-digit literal with a bad checksum, is now a hard error JETH049 in ANY context bare, inside a cast, in arithmetic , exactly as solc rejects it; a 40-digit address literal no longer silently converts to a uintN or bytesN u256 x = 0x<40 hex n is rejected, matching solc ; ARITHMETIC/bitwise on an address literal 0x<40 hex n + 1n is rejected only == / != allowed ; and an uppercase 0X hex prefix is rejected solc accepts only lowercase 0x . Over-acceptance fixed addmod / mulmod : a zero modulus now reverts Panic 0x12 matching solc , not silently returning 0 the raw EVM opcode's behavior . Builtins added: assert cond - Panic 0x01 , keccak256 bytes string , gasleft , blockhash n , <address .balance , address this .balance , block.difficulty = prevrandao . Phase 6 follow-up : a whole MEMORY or CALLDATA static struct assigned to storage this.s = m , this.s = calldataParam , incl. into a mapping value / nested struct field / struct-array element now transcodes the ABI-unpacked image into packed storage byte-identical to solc, incl. packed raw slots . A whole CALLDATA value-element dynamic-array param assigned to storage this.a = p , p: u256 now decodes + validates each element solc-matching dirty-element revert and packed-stores it byte-identical incl. packed raw slots, overwrite-clearing, and dirty narrow/bool/int element reverts . bytes / string mapping keys mapping<bytes, V / mapping<string, V , incl. nested and storage value types now derive the slot as keccak256 keyContent . slotWord solc's dynamic-key rule , verified byte-identical incl. raw slots and empty/long keys. abi.encode ... and abi.encodePacked ... value-type and bytes / string args now produce a bytes value: standard mode mirrors the ABI head/tail tuple encoder; packed mode concatenates each value's byte-width and each bytes / string 's raw content. Verified byte-identical to solc for hashing keccak256 abi.encode... , returning, and storing the result, incl. mixed widths, negative ints, dynamic args, nesting, and empty. Arrays/structs as args, and abi.encodeWithSelector/Signature , remain a later step. A function's .selector is supported: this.f.selector / f.selector on an EXTERNAL or PUBLIC, non-overloaded function - the compile-time bytes4 selector functionSelector signature , left-aligned in the high 4 bytes , byte-identical to solc's f.selector . An internal/private function no ABI selector or an overloaded name is rejected JETH074 . sha256 bytes - bytes32 and ripemd160 bytes - bytes20 precompiles 0x02/0x03 are supported and byte-identical to solc incl. empty/short/long . keccak256 / sha256 / ripemd160 take a single dynamic bytes a string / bytesN is rejected, matching solc; hash a string via keccak256 abi.encodePacked s . Signature recovery, all byte-identical to the matching solc 0.8.35 expansion: ecrecover hash, v, r, s - address is the RAW solc builtin staticcall 0x01: address 0 on any failure, never reverts, no malleability check . recover hash, sig and recover hash, v, r, s are the SAFE OpenZeppelin 5.x ECDSA.recover : the strict s HALF ORDER reject ECDSAInvalidSignatureS , the 65-byte length reject on the bytes form ECDSAInvalidSignatureLength , and the signer == 0 reject ECDSAInvalidSignature , with the exact custom-error selectors. tryRecover hash, sig - bool, address is the never-reverting destructure form. The and staticcall, eq returndatasize, 0x20 guard must bind the success bool first - Yul evaluates and arguments right-to-left, so an inline returndatasize reads the stale pre-call value. The niche crypto precompiles take typed inputs and REVERT on invalid input instead of the raw precompile's silent zero : modexp base, exp, mod - bytes 0x05 ; bn256Add p, q / bn256Mul p, s over a 2- u256 -field G1Point struct / bn256Pairing input: bytes - bool 0x06/0x07/0x08 ; blake2f rounds, h, m, t, f - bytes 64 0x09 ; and pointEvaluation versionedHash, z, y, commitment, proof - fe, modulus KZG 0x0a, destructure-only, 192-byte vh z y commitment 48 proof 48 input matching EIP-4844 . A method named receive { ... } payable implied and one named fallback { ... } are the special entry points, dispatch byte-identical to Solidity's receive / fallback empty calldata - receive; a non-matching selector or value to a non-payable fallback - fallback / revert . Calldata slicing uses a .slice method Solidity's data start:end does not parse in the TS subset : data.slice start , data.slice start, end , and data.slice the whole value on a CALLDATA bytes/string value - a bytes / string parameter, msg.data , a calldata struct's bytes / string field, or another slice. It is a zero-copy calldata sub-view, byte-identical to solc's data start:end : the result is base+start, end-start , .length is end-start , and the bounds check reverts EMPTY iff ! start <= end <= length . Indices must be unsigned a signed index is rejected, matching solc . A slice flows anywhere a calldata bytes value does return , abi.decode / .decode T , keccak256 / sha256 , abi.encode / encodePacked , an event/error arg, address.call {data} , so abi.decode msg.data.slice 4 , T skips the selector. A memory/storage value is not sliceable matching solc . A bytes / string calldata ELEMENT is a first-class calldata value: arr i a BigInt index, e.g. arr 0n can be returned, hashed, measured .length , and sliced arr i .slice ... , and a calldata bytes value can be byte-indexed d i - bytes1 - all byte-identical to solc, including an out-of-bounds Panic 0x32 . String/bytes concatenation matches solc's string.concat / bytes.concat a tightly-packed concatenation, byte-identical to abi.encodePacked of the parts : string.concat a, b, ... and bytes.concat a, b, ... plus the method forms a.concat b, ... . string.concat takes string args and yields a string ; bytes.concat takes bytes / bytesN args and yields bytes . TS template literals are sugar for string concatenation: Hello ${name} desugars to string.concat \"Hello \", name the interpolations must be string -typed, since Solidity has no implicit conversion to string . A computed string a template or a concat is a valid revert / require reason - lowered to a runtime Error string , byte-identical to solc revert string.concat ... . Libraries Phase A: internal, inlined match solc's internal library functions byte-identically: static class L { add a: u256, b: u256 : u256 { return a + b; } } with internal inlined functions, called either qualified L.add x, 1n or - via a @using L decorator on the contract - as an attached method x.add 1n which desugars to L.add x, 1n when x 's type matches the first parameter; Solidity's using L for T does not parse in the TS subset, hence the decorator . Library functions are emitted as ordinary internal functions no separate deployment, no delegatecall, no linking - exactly solc's internal library model , so qualified/attached calls, overloads, library-calls-library, struct/array/string params, and state writes/events through an attached call are all byte-identical. When an attached library function name collides with a genuine BUILT-IN member of the same receiver type .length on any array, push / pop on a dynamic array, .balance / .code / .codehash on address , the member access is AMBIGUOUS and is REJECTED JETH341 matching solc's \"Member ... not unique after argument-dependent lookup\" ; rename the library function or call it qualified L.f x, ... . A library function attached to a DIFFERENT type than the receiver, or whose name is not a built-in member of the receiver type, is not a collision and resolves to the attached function no-collision attached calls still work . Rejected clean : a library with a state field / constructor, L.unknownMember , and an ambiguous attachment. Library constants may depend on sibling constants by bare or qualified name, including forward references. Integer, bool, address, bytesN, string, and bytes dependencies fold byte-identically to solc; cyclic dependencies reject cleanly at compile time. External delegatecall library functions Phase B are also supported, byte-identical to solc's public/ external library functions: an External<T method in a static class library is deployed in the library's own bytecode and called via DELEGATECALL to a link-time address. compile returns the library artifacts + linkReferences ; the deployer substitutes each deployed library's 20-byte address into the contract's $..$ placeholders the contract's Yul uses linkersymbol \"L\" , which solc resolves to the standard placeholder . Verified byte-identical returndata + revert data + logs : qualified + attached external calls, string/bytes/struct/array params and returns, two-library linking, a library mixing internal inlined and external delegatecall functions, and - critically - revert bubbling a string revert and a custom error both propagate through the delegatecall byte-for-byte . A library method may not be payable a delegatecall takes no value ; a pure/view caller of a nonpayable external library function is rejected exactly as solc rejects it. Storage-reference library parameters solc's using For over a mutated storage type remain unsupported - JETH has no storage-reference parameter, so the pattern is not expressible. Minimal proxies EIP-1167 clones - the first of the safe-proxy patterns - are supported via builtins, byte-identical to OpenZeppelin Clones 5.1, and are the only way to deploy a contract from within a contract there is no raw CREATE / new - this is the safe, structured form : clone impl CREATE / cloneDeterministic impl, salt CREATE2 deploy a 45-byte EIP-1167 stub that delegatecalls a fixed impl; cloneWithArgs impl, args / cloneDeterministicWithArgs impl, salt, args append per-clone immutable data to the clone's code read by the impl via cloneArgs - bytes, .decode T for typed args ; predictClone / predictCloneWithArgs give the CREATE2 address; isContract addr - bool. Verified byte-identical to a solc EIP-1167 factory: the deployed clone's runtime bytecode, the delegatecall round-trip runs in the clone's storage , predictClone == the actual deploy address, the cloneArgs round-trip, and clone-storage independence. A deploying builtin writes state, so it is rejected in a view/pure function matching solc . New diagnostics JETH395-397. EIP-1967 upgradeable proxies the foundation of the Transparent / UUPS / Beacon variants are supported, byte-identical to a solc ERC1967 proxy. A @proxy class P { ... } is a proxy contract: JETH generates the canonical delegate fallback forward all calldata to the EIP-1967 implementation slot, 0x360894… . Builtins over the collision-resistant fixed slots: proxyInit impl , admin , initData constructor - code-checks the impl, writes the slot s , runs the init delegatecall once, emits Upgraded address indexed ; upgradeProxy newImpl, data the user gates who may call it ; proxyImplementation / proxyAdmin . A @proxy may not declare state storage belongs to the impl or a user receive / fallback JETH398/399 . Verified byte-identical returndata + the proxy's storage slots + the Upgraded topic/data + revert : calls route into the proxy's storage impl's own untouched , the impl/admin slots, the upgrade-and-event round-trip, the one-time init, the bubbled init-revert, and the auth / isContract gates. The Transparent variant @proxy 'transparent' is supported, byte-identical to OZ TransparentUpgradeableProxy : the generated fallback routes by caller - the admin may only call upgradeToAndCall address,bytes any other admin selector reverts ProxyDeniedAdminAccess 0xd2b576ec , and every non-admin call delegates to the impl so a non-admin call whose selector collides with upgradeToAndCall still hits the impl, defeating the selector clash . A transparent proxy may not declare External<T methods JETH400/401 . The UUPS variant is supported, byte-identical to OZ UUPSUpgradeable : the proxy is the plain @proxy delegate-only and a @uups class carries the upgrade logic - @uups synthesizes upgradeToAndCall address,bytes calls the user-defined authorizeUpgrade newImpl gate; the anti-brick proxiableUUID staticcall on the new impl - a failed/short call reverts ERC1967InvalidImplementation address 0x4c9c8ce3 , a wrong slot reverts UUPSUnsupportedProxiableUUID bytes32 0xaa1d49a4 ; then the EIP-1967 upgrade and proxiableUUID returning the impl slot. @uups without a authorizeUpgrade method is rejected JETH402 . Verified byte-identical returndata + storage + Upgraded event + revert : the authorized upgrade-through-proxy round-trip, both anti-brick reverts, the auth gate, and state preservation. The Beacon variant is supported, byte-identical to OZ BeaconProxy + UpgradeableBeacon : a @beacon class B { constructor impl: address {} } generates the beacon owner = msg.sender at slot 0, implementation at slot 1, an owner-gated upgradeTo address that code-checks the new impl and emits Upgraded address indexed , plus implementation / owner getters; a non-owner caller reverts OwnableUnauthorizedAccount address 0x118cdaa7 , and a @proxy 'beacon' class P { constructor beacon, initData { proxyInitBeacon beacon, ... ; } } is the beacon proxy: its fallback reads the EIP-1967 beacon slot, staticcalls implementation 0x5c60da1b on every call, then delegatecalls, so one beacon.upgradeTo swaps every proxy at once. A beacon proxy may not declare External<T methods or state JETH405-408 . Verified byte-identical returndata + each proxy's own storage + the Upgraded topic/data + revert : per-call routing into each proxy's separate storage, the beacon slot, the implementation / owner getters, the owner gate, and an upgrade-all-at-once over two proxies on one beacon both swap, independent state preserved . The Diamond EIP-2535 multi-facet proxy is supported in all three reference storage layouts, byte-identical to hand-written solc 0.8.35 mirrors. The foundation is @storage 'ns' namespaced storage EIP-7201 : a storage field marked @storage 'ns' instead of a bare state field lives in a struct rooted at base ns = keccak256 abi.encode uint256 keccak256 bytes ns - 1 & ~0xff ; fields sharing a namespace string form one logical struct sequential + packing within it , different namespaces are isolated, and bare state fields stay at sequential slot 0 - all collision-safe verified byte-identical to a solc ERC-7201 struct across scalars, packed fields, mappings, dynamic/fixed arrays, mapping K= struct values, and bytes ; JETH409 gates @storage against a plain state field / static constant / static immutable . A @facet class F { ... } is an ordinary deployable contract whose @storage 'ns' state and External<T selectors are cut into a diamond. A @diamond '<model ' class D synthesizes the whole EIP-2535 surface the user writes only constructor owner { diamondInit owner ; } : the selector-router fallback sload the facet for msg.sig , delegatecall, return-or-bubble, \"Diamond: Function does not exist\" on a miss , an owner-gated diamondCut FacetCut , init, calldata with the model's exact Add/Replace/Remove + the init delegatecall + the DiamondCut event, the four IDiamondLoupe functions facets / facetFunctionSelectors / facetAddresses / facetAddress , ERC-165, and ownership. A @diamond may not declare state / External<T methods / receive / fallback / events all synthesized ; diagnostics JETH411-414. The three models a different storage layout each, identical external surface : - @diamond 'array' the diamond-1/3-hardhat layout : selectorToFacetAndPosition + per-facet bytes4 functionSelectors + address facetAddresses at keccak256 \"diamond.standard.diamond.storage\" ; direct-read loupe, swap-and-pop Add/Replace/Remove. Verified byte-identical to a solc diamond-3 mirror. - @diamond 'packed' the diamond-2-hardhat layout : mapping bytes4= bytes32 facets addr position + mapping uint256= bytes32 selectorSlots 8 selectors packed per slot, MSB-first + uint16 selectorCount ; reconstruct-in-memory loupe. Verified byte-identical incl. a 8-selector multi-slot Add and a slot-crossing Remove . - @diamond 'solidstate' solidstate v0.0.61 : the diamond-2 packing in solidstate's field order selectorInfo / selectorCount / selectorSlugs / fallbackAddress with ownership in separate Ownable/SafeOwnable namespaces, plus solidstate's distinctive features - a settable default fallback address getFallbackAddress / setFallbackAddress ; a selector miss delegatecalls it or reverts Proxy ImplementationIsNotContract and SafeOwnable 2-step ownership transferOwnership sets the nominee, acceptOwnership finalizes . Verified byte-identical to a solc solidstate mirror. Every diamond any model is finalizable : a synthesized owner-gated freezeDiamond permanently disables diamondCut any later cut reverts Diamond: diamond is frozen , with isFrozen to read the state - the EIP-2535-blessed \"deploy upgradeable, freeze later\" immutability path. The frozen flag is appended at a fresh storage slot and the cut guard is a no-op while unfrozen, so a non-frozen diamond stays byte-identical to its reference; the frozen behavior is verified byte-identical to a solc mirror carrying the same flag + guard test/diamond-frozen.test.ts . Now that JETH302 is lifted, a FacetCut constructor parameter is expressible, so a TRUE born-frozen-at-deploy diamond - facets wired once in the constructor, no callable diamondCut - is a possible follow-up; the finalizable freeze above already provides immutability. All diamond/proxy patterns are delegatecall-only with no raw delegatecall / CREATE in user code. A DYNAMIC-field struct a struct with bytes / string fields assigned to storage from a memory local this.d = m or a calldata struct param this.d = p now writes value fields packed and bytes / string fields with overwrite-clear byte-identical incl. raw slots, packing, and long- short overwrite . A struct with a dynamic-ARRAY field from a memory/calldata source stays a clean rejection. static address / bytesN left-aligned / string constants are supported: slot-free compile-time constants substituted at each read site a string as a fresh memory literal , byte-identical to solc and consuming no storage slot. abi.encode accepts every constructable arg type: value, bytes/string, a STATIC struct / fixed-array inline , a DYNAMIC value-element array, a DYNAMIC struct, and nested-dynamic arrays string , T - all offset + recursive head/tail. abi.encodePacked accepts value, bytes/string, and value-element arrays each element padded to 32 bytes, no length; a struct / nested-element array is rejected, matching solc's \"type not supported in packed mode\" . abi.encodeWithSelector bytes4, ... and abi.encodeWithSignature string, ... prepend the 4-byte selector to the standard encoding the signature's selector = keccak256 sig 0:4 , literal or runtime . All verified byte-identical to solc. A NON-indexed STATIC struct / fixed-array event param is encoded INLINE in the ABI data tuple, and a NON-indexed DYNAMIC struct value + bytes/string + dyn value-array fields is encoded as a head offset + head/tail tail. Byte-identical to solc topic0 uses the struct's canonical tuple form; verified for mixed value/struct heads, struct + a dynamic param, nested, packed, and calldata/memory sources . Indexed static struct/fixed-array params - a keccak topic - were already supported. A struct with a dynamic value-element ARRAY field alongside value / bytes/string fields , built in a memory local, now stores to storage byte-identically length + keccak-data slots, overwrite-clearing . The recursive aggregate codec is complete and solc-parity: a struct with a string / T /dynamic value-array field, a nested dynamic struct, DynStruct , Arr<DynStruct,N , and element read/write of a dynamic-element array reached via a struct field or nested array this.d.xs i , this.dd i j , this.o.inner.xs i all compile byte-identical to solc. The practical language surface is complete. Enums + distinctive features F1-F6 - Enums enum Color { Red, Green, Blue } : a Solidity-exact enum ABI uint8 , 1-byte storage packed like uint8 , members 0,1,... . Color.Member constants, comparisons == / != / < /... , Color x conversion range-checked, Panic 0x21 on out-of-range , uN c extraction. Out-of-range enum CALLDATA decode EMPTY-reverts; arithmetic on enums and mixing two enums are rejected. Usable as a state var, param, return, mapping key/value, struct field, event/error arg. Byte-identical to solc. - F1 branded newtypes type TokenId = Brand<u256 : a distinct NOMINAL value type over a value base uintN / intN / bool / address / bytesN , fully erased at codegen/ABI/selectors byte-identical to the base , with wrap/unwrap via TokenId x / u256 t . Mixing brands, or a brand with its bare base, is a type error. - F2 struct spread + for...of : { ...base, field: v } immutable struct update value-field structs and for const v of xs over storage/calldata/memory/fixed arrays, both compile-time desugarings that are byte-identical to the hand-written equivalents. - F3 default + named arguments internal call sites only : a constant default b: u256 = 10n trailing, value-typed, eagerly validated filled when omitted, and named calls this.f { a, b } that bind by parameter name. Defaults never reach the ABI external callers pass every argument . - F4 @nonReentrant : wraps an external/public state-mutating function in an EIP-1153 transient-storage reentrancy mutex OpenZeppelin ReentrancyGuardReentrantCall / 0x3ee5aeb5 on re-entry , no storage slot, never changing the ABI/selector/mutability. - F5 exhaustive switch : switch disc { case L: ... } over a value/enum discriminant evaluated once, desugared to if/else; stricter than TS no implicit fall-through, a non-empty case must terminate, an enum switch with no default must cover every member, duplicate constant labels are rejected . - F6 generics f<T ... : type-safe generic INTERNAL functions, monomorphized at compile time one specialization per concrete value-type instantiation, deduplicated, type-checked per instantiation, byte-identical to a hand-written type-specific function . A generic is never in the ABI. Internal function pointers An INTERNAL function-type value p1: T1, p2: T2, ... = R Solidity's function ... returns R internal type is supported behaviorally byte-identical to solc 0.8.35. Address-taking this.f / f yields a value-typed pointer a stable small integer id identifying the target function ; a call f v through it dispatches on the id a switch over every address-taken target of the matching signature and invokes the target, so apply this.inc, 5n == inc 5 == 6 . Supported surface: a function-pointer PARAMETER and RETURN of an internal bare or private function; a let -bound / state pointer variable; a conditional pointer c ? this.inc : this.dec ; a pointer passed through several functions or returned then called; pure/view/mutating targets the enclosing function's mutability is validated against every same-signature target it may invoke ; f == g / f != g equal iff the same target function . Value-typed signatures only. A call through a NULL/unset pointer reverts Panic 0x51 , exactly like solc's zero-initialized internal function type. REJECTED clean, matching solc : observing the raw pointer as an integer u256 f , returning it as a uint , ABI-encoding a pointer, a funcref in an externally-exposed External<T signature not ABI-encodable , taking the address of an External<T or OVERLOADED function, and external function types / selectors-as-values out of scope . Not supported yet clean reject : an ARRAY or STRUCT of function pointers. Language + type-system features solc-parity sweep - Whole-aggregate storage ops: nested-struct field read/write/copy this.o.inner = D ... , return this.o.inner ; whole fixed-array & struct-with-fixed-array return return this.fa ; whole dynamic-array storage-to-storage deep copy this.a = this.b , this.m k = this.arr , resize + tail-clear ; Arr<D,N / Arr<string,N storage; storage nested dynamic arrays u256 , u256 , string , D with push/pop/length/index/return at the per-inner data slots; struct-with-dynamic-array-field storage ops + calldata echo . - Implicit WIDENING uintN - uintM , intN - intM , bytesN - bytesM , M =N + mixed-width arithmetic/comparison; narrowing / sign change still need an explicit cast. - Explicit casts uintN x / intN x / bytesN x truncate / sign-extend / reinterpret; uint<- bytes same byte size; int<- uint same width . - Exponentiation a b checked, Panic 0x11 ; unchecked: { ... } labeled block wrapping + - and unary - ; ternary c ? a : b short-circuit ; type T .max / .min ; x++ / x-- / ++x / --x statement form ; memory array locals let xs: u256 = a, b value element: index read/write, .length , return , incl. NESTED / multi-dimensional value-leaf arrays see below ; multi-value return f : T1, T2 with return a, b value + bytes/string components . All byte-identical to Solidity. Nested / multi-dimensional MEMORY-array LOCALS value leaves , byte-identical to solc: u256 , Arr<Arr<u256,2 ,2 , Arr<u256 ,2 , u256 , address , new Array<u256 n zero-init: each outer element a pointer to a fresh empty inner array, matching solc , and an array literal .. , .. at any value-leaf depth. A recursive memory codec lays out each level a DYNAMIC outer = len + per-element inline static block / absolute inner pointer; a FIXED-of-dynamic = an N-word pointer table, no length header; a FIXED-of-fixed = inline static words . Element read m i j OOB Panic 0x32 , mutation m i j = v , .length of the outer and an inner, aliasing a whole inner array into a flat local let r: u256 = m i , reference semantics , the whole-array RETURN ABI dyn-/fixed-array encoding , and abi.encode m keccak parity are all byte-identical to solc. Residual clean over-rejections, NOT miscompiles : whole-inner-array ASSIGNMENT m i = ... JETH900 and nested arrays with a bytes / string / struct LEAF bytes , P ; stay JETH200 - the recursive memory codec only lays out value leaves. A nested STORAGE array u256 , and a nested-array CALLDATA param / RETURN at any depth, were already fully supported. Supported now compiles to bytecode + executes on EVM ABI-v2 dynamic codec: NO nesting limit supersedes older \"gated\" notes below The calldata decode and the return/echo encode are a single RECURSIVE codec over the type tree compile-time recursion = unbounded nesting depth; runtime loops for array lengths . Verified byte-identical to Solidity round-trip: solc encodes a known deep value, JETH and solc both echo + index it . Supported as a calldata PARAM and RETURN, at ANY depth: nested dynamic arrays u256 , u256 , ... , arrays of dynamic string / bytes , string , string , dynamic structs struct{...; string; ...} incl. nested + multi dynamic field , a dynamic array of a dynamic struct D , D , and a fixed array of a dynamic element Arr<string,N , Arr<D,N . Element/field access works at depth: m i j k , a i j , ds i .a , ds i .s , with OOB Panic 0x32 at each level. Adversarial calldata inner offsets, =2^255 wraps, ~2^64 lengths matches solc byte-for-byte lazy-access signed wrap; echo unsigned cap + Panic 0x41 alloc . Returning a WHOLE STORAGE aggregate whose field/element is dynamic also works via the storage-source twin abiEncFromStorage return this.d dynamic struct, return this.ss string , return this.recs D , byte-identical incl. short/long-string transitions, AND returning a whole MAPPING value return this.m k struct / dynamic struct / value array / string encoded from the runtime keccak key . base slot. Surface / declarations - A bare leaf class C { ... } - contract. - Bare fields x: T - storage slots Solidity-compatible layout + packing . - Methods - ABI functions. - Visibility: an External<T or Payable<T return marker is the SOLE way to expose a function an ABI entry point ; a public state getter is a Visible<T field. A method / field WITHOUT such a marker is INTERNAL private-by-default : callable by name from inside the contract, never in the ABI, not externally callable; a private member is spelled with a leading . The legacy @public / @internal / @private decorators are removed JETH481 , pointing at the native spelling and @hidden is rejected JETH440 - the compiler owns the internal-side decision private now; private vs internal inferred from cross-contract use once inheritance lands . A Visible<T field gets an auto-generated getter and is still usable in code. To expose logic that also recurses / is reused internally, write an External<T wrapper over an internal impl. - Mutability is INFERRED from the body no marker : a read-only function resolves to pure touches no state/env, transitively or view reads, never writes , a read-only value-returning external is spelled with get get f : External<T , else JETH352 , and a payable function returns Payable<T . A transitive write in a would-be read-only function is rejected JETH056 . Inside an interface - and ONLY there a class body is inferred instead, JETH498 - the View<T / Pure<T markers state the declared mutability, alongside Payable<T . All inference resolves to a concrete visibility+mutability before ABI emission, so the generated ABI is the true one. - Constant state initializers a bare x: u256 = 42n field - written in creation code. Types - u8 .. u256 , i8 .. i256 BigInt literals only . - bool , address , bytes1 .. bytes32 . - mapping<K,V , T , and Arr<T,N are fully laid out AND code-generated indexing, packing, push/pop, keccak slot derivation; see the Mappings / arrays sections below . - enum Name { ... } a 1-byte uint8 -backed enum, ABI uint8 ; see \"Enums + distinctive features\" . - Branded newtypes type X = Brand<Base nominal value types, F1; see \"Enums + distinctive features\" . Expressions / statements - Checked arithmetic + - / % default - Panic 0x11 overflow, Panic 0x12 div/mod-by-zero. - Comparisons < <= = == != , bitwise & ^ ~ , shifts << , logical && ! . - Compound assignment += , -= , ... . - this.stateVar read/write full-word and packed . - Local let with explicit type; parameters static value types . - return , expression statements. Control flow Phase 2 + F2/F5 - if / else / else if , for init; cond; post , while , do...while , break , continue . - for...of over an array F2 : for const v of xs storage/calldata/memory/fixed arrays , desugared to an indexed loop re-reads length + element each iteration . for...in is rejected. - switch F5 : switch disc { case L: ... default: ... } over a value/enum discriminant evaluated once, with exhaustiveness checking over enums and no implicit fall-through see \"Enums + distinctive features\" . Solidity has no switch ; JETH's desugars to if/else. - Lexical block scoping with a scope stack. A nested local may shadow an outer-scope variable like solc, which warns but accepts ; a redeclaration in the SAME scope is rejected JETH068 . Each declaration gets a unique Yul name, so emitted Yul is always shadow-free. Labeled break/continue rejected. Fall-through returns the zero value matches Solidity . Reverts & custom errors Phase 2 - require cond , require cond, \"msg\" , revert , revert \"msg\" - byte-exact Error string / empty revert verified vs solc; UTF-8 length, word padding . - Custom errors: a file-level type Name = error<{ p: T } or a Name: error<{ p: T } member ; revert Name args / require cond, Name args - selector + ABI-encoded static args. Error args are evaluated eagerly a side-effecting arg reverts even when the condition passes , matching solc. type:\"error\" entries emitted in the ABI. Events Phase 2 - A file-level type Name = event<{ p: indexed<T ; q: U } or a Name: event<{ ... } member ; emit Name args ; . - topic0 = keccak256 canonical sig ; LOG nIndexed+1 ; indexed params - topics int sign-extended, bytesN left-aligned, bool/uint/address as the word ; non-indexed params - ABI data region in declaration order. Rejected in view/pure functions. type:\"event\" entries with indexed flags emitted in the ABI. Mappings Phase 3 - mapping<K,V storage, including nested mapping<K, mapping<K2,V . - this.m k read/write/compound assign; slot = keccak256 keyWord . p per level recursive , byte-identical to Solidity differentially verified, incl. raw slots . - Value types and keys: uintN/intN/bool/address/bytesN narrow values packed at the derived slot via read-modify-write, like solc . Keys hashed in register form uint zero-ext, int sign-ext, address zero-ext, bytesN left-aligned . Environment globals Phase 3 - msg.sender CALLER , msg.value CALLVALUE, payable only , msg.sig selector , tx.origin ORIGIN , address this ADDRESS , and block.timestamp/number/chainid/ coinbase/basefee/gaslimit/prevrandao . Forbidden in pure functions except msg.sig . Payable & address Phase 3 - Payable functions a Payable<T return marker accept ETH no callvalue guard ; non-payable reject it empty revert . - address 0n literal; payable x ; address<- uint160 no-op and address<- bytes20 96-bit shift casts. Address comparisons are unsigned; address arithmetic is rejected. Dynamic bytes / string Phase 4a - Storage short <32 inline / long =32 with keccak p data slots; overwrite-clearing , calldata params bounds-checked decode , ABI return encode head/tail , .length bytes , b i - bytes1 with Panic 0x32 , dynamic events + runtime Error string . Byte-identical to Solidity incl. raw slots. - mapping<K, bytes / mapping<K, string scalar dynamic value incl. nested mapping<K, mapping<K2, bytes : the value lives at the runtime keccak key . base mapping slot short inline / long at keccak slot . Read / return / write this.m k = v , overwrite-clearing the old tail / .length bytes / b i byte-index with Panic 0x32 , all byte-identical to Solidity short + long, empty, shrink/grow . Dynamic arrays T Phase 4b - Storage whole-slot, packed, unpacked- address , push / pop / .length / a i / a i =v with Panic 0x32/0x31/0x41 , pop zeroes freed slot/byte, calldata decode, ABI encode unpacking packed storage to full words , mixed static+dynamic args. Structs Phase 4c - A struct type type Name = { ... } ; mixed-width field packing Solidity-identical slots ; this.s.field read/write RMW ; positional Name ... construction, incl. nested Outer p, Inner a,b , q flattened into packed slots, Phase 4e-2c ; whole-struct assignment; struct - ABI tuple return. - A struct may have a MAPPING field G7 : type Acct = { head: u256; bal: mapping<address, u256 } . Such a struct is STORAGE-ONLY matching solc : allowed as a state field or a mapping VALUE mapping<K, Acct , accessed via this.s.bal a / this.m k .bal a the mapping base is the field slot structBase + fieldSlot ; value at keccak key . base ; the mapping field never packs with neighbours its own slot . Byte-identical to solc on raw storage slots. A struct containing a mapping cannot be returned, a function param, constructed, copied, or a memory local JETH247 . - Whole-struct assignment into a STORAGE aggregate slot static or dynamic struct : a mapping value this.m k = Name ... , an array element this.recs i = Name ... / this.fa i = P ... / this.md k i = D ... , and a state var this.d = D ... . Both the constructed-literal form and the storage-to-storage COPY form this.m a = this.m b , this.recs i = this.recs j , this.d = this.m k , cross-source : writeStruct / copyStruct land at the runtime element/mapping slot, clearing each dynamic field's old tail long- short shrink, grow byte-identically to solc. - Whole-struct array-element READ / RETURN return this.recs i / this.fa i / this.md k i encoded from the element slot by the storage-source recursive encoder static = inline tuple, dynamic = 0x20 + head/tail . Bounds-checked Panic 0x32 ; const OOB is a compile error . Fixed arrays Arr<T,N Phase 4c - Inline storage whole-slot + packed, straddle-free ; a i read/write; .length constant ; runtime Panic 0x32 ; constant out-of-bounds is a compile error. Nested storage access Phase 4c-3 - Unified AccessPath field / index / mapKey steps : this.s.inner.x , this.pts i .x , this.m k .field , this.arr i .sub j , this.m k .arr i , this.m k i .field , this.mat r c . Whole-slot index elements; byte-identical raw slots + OOB Panic 0x32 . Storage dynamic array of struct Phase 4e-2 - A state field recs: Rec the \"list of records\" pattern : this.recs.push Rec ... , push zero element , this.recs.pop , this.recs i .field read/write RMW, incl. nested-struct and fixed-array element fields , this.recs.length . Element at keccak p +i storageSlotCount struct , fields packed Solidity-identically. OOB Panic 0x32 , pop-empty Panic 0x31 ; raw slots byte-identical. Mapping-valued dynamic arrays Phase 4e-2b - mapping<K, T value or struct element : this.m k .push ... / .pop / .length , this.m k i read/write and this.m k i .field RMW. Length at the runtime mapping slot keccak key . base , data at keccak lenSlot ; per-key isolation, byte-identical to Solidity. Array of dynamic elements string / bytes Phase 4e-4 - string / bytes as a calldata PARAM and as a RETURN identical layout : whole-array echo return a , head/tail re-encode with a per-element offset table whose base is the table start , a i re-encoded as a standalone top-level string/bytes , and a.length . Outer offset base = calldata byte 4; element offsets relative to the table start word after the length word ; two inclusive range checks per dynamic level; i = len - Panic 0x32 ; any layout fault - EMPTY revert. Byte-identical to Solidity differentially verified . These COMPOSE through the recursive codec 4e-5 : string / string and a fixed Arr<string,N = string N also work as a calldata PARAM and RETURN, with a i / a i j element access and .length . A string or bytes inside a STRUCT works as a whole-struct echo / return; the one piece still gated is ELEMENT access into such a field on a calldata param s.xs i - JETH230 . Storage / mapping-valued string / bytes - State fields ss: string / bytes and mapping<K, string / mapping<K, bytes : layout mirrors solc length at slot p / runtime mapping slot keccak key.base ; element header i at keccak lenSlot +i , a normal storage bytes/string: short <32 inline, long =32 with keccak headerSlot data slots . Supports this.ss.push s / .pop / .length , this.ss i read re-encoded as a standalone top-level string/bytes on return + write this.ss i = s , overwrite- clearing of the old element's data slots , this.bb i .length and a byte index this.bb i j , and the same for the mapping-valued form per-key isolation . Each element reuses the storage bytes/string codec; pop fully clears the freed header AND its data slots. OOB index Panic 0x32 , pop-empty Panic 0x31 , push past 2^64-1 Panic 0x41 . Byte-identical to Solidity incl. raw storage slots differentially verified . Nested dynamic array T Phase 4e-5 - u256 / u8 a dynamic array of dynamic value arrays as a calldata PARAM and as a RETURN: whole-array echo return m , head/tail re-encode with a per-inner pointer table whose base is the pointer-region start = the word after the outer length word , m i j element read, m.length outer count , m i .length inner count . Outer offset base = calldata byte 4; inner-offset base = the word after outer len spec section 2 . u8 is byte-identical to u256 except element validation m i j element 255 - EMPTY on read; the echo CLEANS value elements, matching solc's array copy . Index OOB on either dimension i = outerLen or j = innerLen - Panic 0x32 ; any layout fault bad outer / inner offset, inner length implies elements past calldatasize, truncated pointer table, wrong-base offset - EMPTY revert. Overlapping / non-canonical inner offsets are accepted pure pointer arithmetic . Byte-identical to Solidity differentially verified . - STORAGE array compositions G6 : Arr<T ,N = uint256 N , a fixed array of dynamic arrays and Arr<T,N = uint256 N , a dynamic array of fixed arrays, incl. packed fixed elements like uint8 4 work as state fields: element access, .push , .length , and nested indexing a i j , byte-identical to solc incl. raw storage slots. A whole calldata-param or return of these composite shapes stays gated. Dynamic array of static struct Phase 4e-1 - Pt as a calldata param Pt a static struct : whole-array echo return ps , head/tail decode + re-encode , element value-field read ps i .x , ps.length . ABI-unpacked elements one word per leaf; stride = abiHeadWords 32 , stride-aware decode payload bounds, OOB Panic 0x32 . Lazy field-read validation dirty leaf reverts empty ; a whole-struct-array echo VALIDATES every field vs a value-array echo which cleans . Storage Pt and ps i whole-element are still deferred. Storage / mapping-valued dynamic structs Phase 4e-7 - A struct with =1 bytes/string field a DYNAMIC struct in STORAGE or as a mapping value: solc-identical layout contiguous slots; each static field uses normal packed storage; each bytes/string field at base + fieldSlot is a normal storage bytes/string: short <32 inline, long =32 with keccak headerSlot data slots . Supports a bare state field d: D , a mapping<K, D value, a dynamic array of dynamic struct recs: D and a mapping<K, D value , and a nested Outer{x; D inner; y} . Operations: this.d.field read/write for both a static field packed RMW and a bytes/string field storeStrMem , overwrite- clearing the old tail ; whole-struct assignment this.d = D a, s each field written in declaration order; the calldata string param is validated upfront, so a malformed arg reverts before any storage write, byte-identical to solc, no partial write ; this.recs.push D a, s / push / .pop / .length , this.recs i .field read/write; the same via a mapping key; the bytes field's .length and a byte index this.e.b j / this.recs i .b j . pop fully clears the freed element including each bytes/string field's keccak headerSlot long data slots verified vs solc . OOB index Panic 0x32 , pop-empty Panic 0x31 , push past 2^64-1 Panic 0x41 ; a malformed-calldata push/set reverts EMPTY with no slot written. Byte-identical to Solidity incl. raw storage slots differentially verified . Returning a WHOLE storage dynamic struct return this.d , return this.m k stays gated JETH232: read its fields ; D as a calldata param / return, and fixed Arr<D,N of a dynamic struct, stay gated. Dynamic structs Phase 4e-6 - A struct with =1 dynamic field bytes/string, or a nested struct that is itself dynamic is a DYNAMIC struct spec section 3 and is supported as a calldata PARAM and as a RETURN, byte-identical to Solidity. Static fields stay INLINE in the tuple head declaration order ; each dynamic field gets a head OFFSET word whose base is the TUPLE START. Supports: reading d.staticField lazy dirty-bit validation - EMPTY , d.dynField bytes/string , e.name.length , e.name i bytes index, OOB - Panic 0x32 ; echoing a dynamic struct return d , decode + head/tail re-encode, static fields VALIDATED ; constructing + returning one return D a, s , literals included ; nested Outer{x; D inner; y} field reads / echo / construct each container's offsets reset to ITS tuple start ; multiple dynamic fields in one struct e.g. {a; string s; bytes b; z} . The param head word = offset to the tuple base byte 4 ; per-tuple field offsets relative to the tuple start spec section 3.2 ; bounds checks per dynamic level; any layout fault offset/length past calldatasize, truncated head, wrong-base offset - EMPTY revert; field reads are LAZY a malformed UNREAD dynamic field is ignored . Differentially verified. A struct with a dynamic-ARRAY field T / string / T is now supported in storage, as a whole-struct RETURN, and as a whole-struct calldata-param echo; only ELEMENT access into such a field of a calldata struct param s.xs i stays gated JETH230 . D a dynamic array of dynamic structs and fixed Arr<D,N as a calldata param/return are gated cleanly. Aggregate calldata params Phase 4d - struct and fixed-array Arr<T,N function parameters, decoded lazily from the ABI- unpacked head one 32-byte word per leaf; head cursor advances by abiHeadWords , nested structs flattened inline . Field / index / nested reads p.x , a i , o.inner.b , t.data j , ps i .y , constant + runtime indices, .length . Lazy per-access validation: a dirty leaf read reverts empty, an unread dirty leaf is ignored; runtime OOB Panic 0x32 ; short calldata reverts empty. Mixes with value and dynamic T / bytes siblings head cursor + byte-4 tail base . Backend / interop - Yul IR - solc Cancun - creation + runtime bytecode. - ABI JSON emitted from the analyzer; canonical 4-byte selectors. - Static value-type calldata decoding with strict input validation dirty high bits on uintN/intN/bool/address/bytesN revert, matching Solidity 0.8 + single static return encoding. - Packed storage incl. left-aligned bytesN is byte-identical to Solidity differentially verified . - Short-circuiting && / RHS not evaluated when it can revert . - Non-payable functions reject ETH; unknown selector reverts. Post-sweep surface detail what's supported, with the remaining gates inline This section records the detailed post-Phase-4 surface beyond the headline sections above: the solc-parity sweep CLOSED the earlier \"storage aggregate\" gaps whole-struct field read/write/copy, whole fixed-array/struct return, whole dynamic-array copy, Arr<D,N / Arr<string,N storage, storage u256 / string / D / T , struct-with-dynamic-array-field, general numeric/bytes casts, implicit widening, , ternary, unchecked , type T .max , ++ / -- , memory value-array locals, multi-value return . The bullets below describe what is now SUPPORTED in each area, calling out the few pieces still gated inline; the genuinely-unsupported items are consolidated in the \"Still gated\" list at the end. Every gate rejects with a precise diagnostic and is never miscompiled. - Calldata struct-with-dynamic-array-field FIELD ACCESS s.xs i on a param ; the whole-struct ECHO of such a param works. - STATIC struct MEMORY locals are supported G9 : let p: P = P ... construct, value-field read/write including nested chains p.x , p.inner.x , d.o.inner.a , p.x = v , p.x += v , p.x++ , whole-struct return, and memory aliasing let q = p ; a write through q is visible through p . internal / private functions take and RETURN static structs as memory by reference mutation in a callee is visible to the caller ; a struct can be passed, returned, bound to a local, chained, and built via recursion. Also supported: copying a memory local FROM a storage struct or calldata struct param let p: P = this.s / = calldataParam , a fresh COPY ; reading a whole nested struct field as a value return p.inner , aliasing ; bytes / string memory locals let s: string = X : return, .length , b i , alias ; FIXED-ARRAY-of-value memory locals let a: Arr<u256,3 = ... : a i read/write, return, alias, storage/calldata copy . DYNAMIC-field struct memory locals are also supported G10 when every field is a value type or bytes / string no static-array, nested-struct, or dynamic-array fields : let d: D = D x, str construct, value-field read d.a and write d.a = v , dynamic-field read d.s whole, d.b.length , d.b i , and whole-struct return d . The image is a pointer-headed tuple value fields inline, bytes / string fields a len data pointer , so return d reuses the dynamic-struct tuple encoder via a memory TupleSrc ; dynamic fields may be built from a memory-string local alias or a string literal. A dynamic-field struct memory local may also be COPY-initialized from a storage struct let d: D = this.st / this.m k / this.recs i , a calldata struct parameter let d: D = x , decoded + validated into a fresh image , or ALIASED from another struct local let e: D = d , a Solidity memory reference ; and a bytes / string field may be WRITTEN d.s = x , re-pointing the head word at a fresh blob . A NESTED-STRUCT field works fully in a struct memory local construct + deep read . A STATIC struct memory local may also be declared WITHOUT an initializer let p: P; : it is zero-initialized every value field 0, bool false, enum member 0, nested static struct + fixed array all-zero exactly like solc's P memory p; , then field-assigned byte-identical, verified on returndata + raw storage . Member access whose BASE is a struct-returning internal call is also supported this.mk a .x / mk a .x : the call result is materialized to a memory image and the VALUE field is read byte-identical to solc . Still gated: a struct memory local with a DYNAMIC-array field JETH200 , and ELEMENT access into a FIXED-array field through the local s.a i , JETH900 ; a NON-value field of a struct-returning call result this.mk a .inner , JETH245 - bind the call to a local first ; a struct param to a PUBLIC/ EXTERNAL callee via an internal call an external/message call, Phase 6 ; new T n use an array literal . A calldata struct-ARRAY element bound to a memory struct local let p: P = ps 0n / for const p of ps , NESTED / multi-dim value-leaf memory-array locals u256 , Arr<Arr<T,N ,M , new Array<u256 n , the aggregate abi.decode STRUCT + tuple-with-struct targets via the pointer-headed buildDynStructFromMemBlob decoder , and the aggregate abi.decode ARRAY targets - a STATIC-struct array P , decoded inline via abiDecFromMem , bytes / string , and nested value arrays u256 , u256 , Arr<u256 ,N , decoded into Residual B's absolute- pointer image via the new abiDecFromMemToImage , plus the u256, P tuple form - are all now SUPPORTED, byte-identical truncated/malformed blobs revert exactly like solc's memory decode . REMAINING residuals clean over-rejections, never miscompiles : a whole-inner-array ASSIGNMENT m i = ... to a nested memory array JETH900 ; a nested array with a bytes / string /struct LEAF bytes , P , JETH200 ; and the still-deferred abi.decode ARRAY targets - a DYNAMIC-struct array P P with a bytes / string /dyn-array field and nested-aggregate arrays P , bytes - JETH322/JETH200, pending the B3/B4 memory representations. - Tuple destructuring works: let a, , c = src and a, , c = src where src is a multi-value internal call this.f or a tuple literal x, y , e.g. swap a, b = b, a ; new locals, existing value lvalues incl. storage, or skipped components; value components only. - The delete x statement works on every storage location value/packed/struct/array/dynamic/bytes/ string/mapping-value/nested-place/local , leaving mappings intact; delete of a whole mapping is rejected parity with solc . - A packed <256 -bit element of a nested DYNAMIC array this.m k .dynArr i ; the packed FIXED-array case through a struct field this.q.pts i now works runtime byte offset . - A ternary over bytes / string c ? a : b , literal/storage/calldata branches and over a STATIC struct or fixed array c ? this.x : this.y , materialized + pointer-selected, incl. nested ternary now work; only a DYNAMIC storage struct/array ternary stays gated select before the aggregate op . - A whole DYNAMIC calldata array / struct param as a COMPONENT of a multi-value return return calldataArrayParam, x works, as do element components incl. a string element and storage / memory-array components; only a WHOLE STATIC calldata aggregate Arr<T,N or a static struct param as a component stays gated JETH900 . - Whole FIXED-array storage copy this.g = this.src , whole STORAGE-source inner-array assignment this.dd i = this.other , and reading a whole fixed-array return this.g i work; assigning a whole FIXED-array element in place this.g i = arr and a CALLDATA-source inner-array assignment this.dd i = xs stay gated. - Mixed calldata composite element access works: uint256 2 dynamic-of-fixed and uint256 2 fixed-of-dynamic support a i , a i j , a i .length , and whole-param echo JETH151/210 , byte-identical incl. malformed-offset/length EMPTY-revert and full N-word head readability. - Standard tuple ABI JSON: struct params/returns render as t1,t2 in the JSON type field rather than type:\"tuple\" + components selectors are canonical and correct; JSON-shape polish . - msg.data is the whole calldata as bytes selector included, so msg.data.length == calldatasize : .length , copy to a memory bytes / return, and byte-indexing Panic 0x32 OOB ; allowed in pure functions calldata, like msg.sig . - An indexed FIXED-array or static-struct event param is a keccak topic of abi.encode value from a a state-field source or a calldata-param source . Indexed bytes / string and indexed DYNAMIC value-element arrays also work keccak of the content / element words . All byte-identical to solc. - static constant fields: a slot-free compile-time constant uintN/intN/bool inlined at each read site no SLOAD, no storage slot, absent from the ABI , byte-identical to solc incl. raw storage layout. - Evaluation ORDER of side-effecting subexpressions now matches solc: BINARY operands evaluate RIGHT-to-LEFT and ARGUMENT lists array literals, return tuples, event/error args, call args LEFT-to-RIGHT, byte-identical to solc verified . This covers ++ / -- in value position and assignment-expressions x = v / x += v / x = y = a . - Internal/private/public function calls are supported this.method ... or bare name ... for value-typed and void params/returns plus static-struct params/returns to internal / private callees , with recursion, mutual recursion, and transitive view/pure purity. A MULTI-VALUE internal call value return components is callable via tuple destructuring below . Aggregate array/bytes/string params/returns through an internal call remain gated. At internal call sites, default arguments f a, b = 10n and named arguments this.f { a, b } are supported F3 , and generic functions f<T ... are monomorphized per concrete value-type instantiation F6 . - Tuple destructuring: let a, , c = src declaration, new locals and a, , c = src assignment to existing value lvalues incl. storage; omitted slots discard the component , where src is a multi-value internal call this.f or a tuple literal x, y , e.g. swap a, b = b, a . The RHS is fully evaluated before any store. Value components only an aggregate/bytes component in a tuple is gated . Byte-identical to solc. - Phase 5 functions in depth - constructors, immutables, modifiers byte-identical to solc incl. raw storage slots : a constructor params { body } runs once at deploy - value-type params uintN/intN/bool/address/bytesN/enum/branded are ABI-decoded from the args appended to the init code decoded from memory , the body may write state and read msg.sender / msg.value payable / address this , constant field initializers run before it, and a non-payable constructor rejects deploy-time value. Immutable value-type fields a ctor-assigned static K: T; are assigned in the constructor and baked into the runtime code via setimmutable / loadimmutable - they consume NO storage slot a constructor read sees the staged value, a runtime read is loadimmutable , and reading one needs view not pure . User @modifier s applied via @name / @name args inline their code around the body - both PRE-code a guard like require cond ; ; and POST-code after . Post-code with an early return uses solc-identical buffered-return semantics: the body's return runs the enclosing modifier post-code inner-first, from any depth incl. inside a body loop before the value is encoded and returned once the body is lowered as a synthesized Yul function so return becomes ret := v; leave . Multiple modifiers nest leftmost-outermost, the same modifier may apply twice, arguments evaluate exactly once a modifier param never shadows a same-named function param in the body , their effects feed the purity fixpoint, and they compose with @nonReentrant . A bare return in a modifier exits only that modifier layer, so an enclosing modifier resumes after its ; this also works inside loops and constructor modifiers. A @modifier may also decorate the constructor the canonical base-init guard, e.g. @onlyValid constructor ... { ... } . The identifier is reserved the modifier placeholder and cannot be a declared name JETH034 , matching solc. - Phase 6 IN PROGRESS, each byte-identical to solc : external low-level calls addr.call / tryCall / staticcall / code / codehash / revertWith , abi.decode + <bytes .decode + a decode: call option , typed interface calls IFoo addr .bar x , try / catch , new Array<T n . STILL TO DO: inheritance is / virtual / override / super + base ctors , libraries using for / DELEGATECALL , ecrecover + remaining precompiles, receive / fallback , function types, bytes / string.concat , calldata slicing, new contract / CREATE2, and source maps / CLI polish. address.transfer / .send are deliberately omitted - the safe pattern is CEI + @nonReentrant over a full-gas addr.call . Still gated the complete list of what is rejected with a diagnostic, never miscompiled Each of the following compiles to a clean compile-time error verified , not a miscompile: - ELEMENT access into a dynamic-array field of a calldata struct param s.xs i where xs: u256 / string - JETH230. The WHOLE-struct echo / return of such a param works. - Aggregate array / bytes / string params or returns through an internal call - JETH242. Value-typed and static-struct params/returns to internal / private callees work. - A struct param to a PUBLIC/EXTERNAL callee via an internal call - JETH242 that is a message call, Phase 6 ; a struct param to an internal / private callee works by-reference memory . - A struct memory local with a DYNAMIC-array field u256 / string - JETH200; and ELEMENT access into a FIXED-array field through such a local s.a i - JETH900. Value-typed, bytes / string -field, and NESTED-STRUCT-field struct memory locals work fully G9/G10 . - A whole calldata/memory struct copied into storage when it contains a dynamic array with a fixed-inner value-array level uint256 2 , address 2 , and deeper variants - JETH470. Storage-to-storage copy of the same shape remains supported. - new T n - JETH023 use an array literal let xs: u256 = ... . - In-place assignment of a WHOLE fixed-array element this.g i = arr - JETH226 whole fixed-array storage copy this.g = this.src and whole STORAGE-source inner-array assignment this.dd i = this.other work; a CALLDATA-source inner-array assignment this.dd i = xs is gated . - A WHOLE STATIC calldata aggregate Arr<T,N or a static struct param as a multi-value-return component - JETH900. A whole DYNAMIC calldata array/struct component, element components incl. a string element , and storage/memory components all work. - A ternary over a DYNAMIC storage struct / array - JETH074 select before the aggregate op; ternary over bytes / string and over a static struct/fixed array works . - A packed <256 -bit element of a NESTED dynamic array this.m k .dynArr i the packed fixed-array-through-a-struct-field case this.q.pts i works . - Tuple ABI JSON shape : struct params/returns render as t1,t2 rather than type:\"tuple\" + components selectors are canonical and correct; this is a JSON-shape gap, not a behavior gap . - Phase 5 constructor / immutable / modifier - now at solc parity the main over-rejections were lifted, byte-identical to solc : a constructor with an aggregate/dynamic param uint / bytes / string /struct - JETH302 , a constructor that calls an internal function JETH303 , an an immutable field exposed with a getter and its auto-generated view getter JETH312 , a @modifier with its placeholder inside a conditional the 0-or-N-times shape; skipping returns the function's zero value - JETH321 , and an inline-initialized immutable field JETH311 are all now SUPPORTED. The REMAINING clean gates rare shapes; each a diagnostic, never a miscompile : a defaulted ctor param JETH304 - JETH-specific, no solc form ; a non-value-type immutable JETH310 and an immutable assigned outside the constructor JETH313 are accept/reject PARITY solc rejects too ; and a few rare modifier shapes not yet lifted - a value-return inside the modifier body JETH324, parity , and a POST-code modifier on an aggregate/dynamic-param or multi-value/aggregate-return function or constructor JETH323 . One known low-severity over-rejection: a constructor that provably overflows a staged immutable-field read at runtime is rejected JETH901 where solc accepts and the deploy then reverts the contract is non-functional in both compilers . - Phase 6 remaining external low-level/message calls, abi.decode , interface calls, try / catch and new Array<T n are DONE : inheritance, libraries using for / DELEGATECALL , abstract contracts, ecrecover + remaining precompiles, receive / fallback , function types, bytes / string.concat , calldata slicing, new contract / CREATE2, address.transfer / .send deliberately omitted , source maps / CLI polish. Permanently rejected no on-chain meaning number /floats, any , async/await, generators, closures/free functions, throw , try/catch, regex, template literals, typeof / instanceof / in , array/call spread/rest ...a , f ...a , eval . Object spread { ...base, x: v } in a struct literal IS supported, F2. delete - delete x Solidity storage reset to the type's zero value is supported on storage value vars packed-aware: a packed field zeroes only its lane , structs value fields zeroed, bytes/string fields cleared, nested struct/array fields recursed, MAPPING fields left intact - matching solc , fixed arrays, dynamic arrays data slots zeroed + length 0 , bytes/string header + long-data slots freed , mapping VALUES delete this.m k , incl. struct/bytes values and this.m k .field , nested places delete this.s.f , delete this.a i , and local value variables. delete of a WHOLE mapping is rejected parity: solc also rejects it . Verified byte-identical to solc on returndata AND raw storage slots incl. computed keccak data slots . Known JS-vs-EVM divergences flagged directive §9 \"known danger\" - a + b is checked 256-bit integer add, never JS + no string concat, no floats . - Integer literals must be BigInt 1n , never 1 . - == is value equality on 256-bit words no JS coercion ; maps to EVM EQ . - No implicit numeric conversions between integer widths or between bool and int enforced in expressions AND state initializers . - && / short-circuit: the RHS is not evaluated when the result is already determined, so a RHS that would revert e.g. division does not run, matching Solidity. - A nested local may shadow a visible outer variable accepted, matching solc, which warns but compiles ; a redeclaration in the SAME scope is rejected JETH068 , and disjoint sibling blocks may reuse a name. Each declaration gets a unique Yul name so emitted Yul is always shadow-free, so shadowing never miscompiles. The for-of and switch desugars mint their temps with a counter that skips past every visible user name, so a user variable spelled like an internal temp is never hijacked. - require / revert custom-error arguments are evaluated eagerly unconditionally , so an arg that reverts fires even when the condition passes - matches solc, not JS short-circuit intuition. - msg.value is readable only in payable functions matches solc; it is NOT silently 0 elsewhere . Reading environment globals msg. / block. / tx. / address this is forbidden in pure functions. - address is a distinct value type: comparisons are unsigned, arithmetic is rejected, and address / address payable share one EVM word payable- plain is implicit, the reverse needs payable ... . Intentional, safer-than-solc deviations adversarial calldata only For honest, ABI-encoded calldata JETH is byte-identical to solc everywhere verified by the differential suite . The differences below arise only with hand-crafted, malformed calldata that no conforming encoder produces; in each case JETH errs strictly safe it rejects , so a contract's behavior toward real callers is unaffected. They are documented rather than matched because replicating solc's signed-offset / allocator footguns would add no honest-caller value while risking the verified happy path. - A dynamic-container INNER offset a tuple member, a T inner pointer, a string / bytes element offset whose value has the high bit set = 2^255 : solc treats it as a signed/negative offset, wraps the pointer mod 2^256, reads the out-of-range word as zero, and proceeds with a zero-length payload returning empty or Panic 0x32 on a later index . JETH rejects any such offset with an EMPTY revert. Offsets in 2^64, 2^255 revert in BOTH compilers. - A dynamic length near 2^64-1 in a decode-to-memory copy: solc's allocator overflows and raises Panic 0x41 ; JETH's bounds check raises an EMPTY revert first. Both fail the call; only the revert form differs. - Deep internal-call RECURSION live depth in roughly ~340, ~1100 : solc lowers internal calls to EVM-stack frames and hits the 1024-slot stack limit, reverting with \"stack overflow\" gas-independent ; JETH lowers internal calls to Yul functions whose frames live in memory, so it computes the correct value where solc aborts. The exact per-function ceiling depends on solc's register allocation e.g. ~338 frames for a one-extra-local callee , which is impractical to replicate from a Yul backend. For all realistic call depths the two are byte-identical; only pathological recursion hundreds of frames, astronomical gas differs, and there JETH is strictly more capable correct result vs solc revert .","path":"reference/feature-matrix/"},{"title":"Product roadmap","group":"Project","text":"Product roadmap Built by @a16k-lab https://github.com/a16k-lab . This roadmap turns JETH from a strong compiler research project into a product that can be installed, integrated, reviewed, and operated safely. The order is intentional: release trust comes before adding a large new language surface. Release principles 1. No known miscompiles at release. 2. Accepted behavior has executable evidence. 3. Unsupported behavior rejects cleanly. 4. Builds are reproducible and attributable to an exact toolchain. 5. Documentation and artifacts are versioned with the compiler. 6. Security claims are scoped and independently reviewable. R0: release foundation Goal: make the existing compiler installable, deterministic, and supportable. - Resolve package name, ownership, public/private status, and the GPLv3 versus MIT metadata mismatch. - Add a bin entry and publish jethc as a versioned package, then provide checksummed standalone binaries. - Define semver, compiler/language versioning, changelog, compatibility window, and deprecation policy. - Add CI across supported operating systems and Node versions. - Generate reproducible artifacts, checksums, SBOM, and signed provenance. - Add SECURITY.md , a private reporting channel, and an incident process. - Freeze the first artifact schema and EVM-target policy. Exit criteria: a clean checkout can install a pinned release, compile an example, reproduce published artifacts, and report a security issue without relying on a developer workstation. R1: professional CLI and developer experience Goal: make JETH pleasant and dependable in real projects. - Project config, multi-file builds, contract selection, imports, and output profiles. - Structured diagnostics, snippets, stable exit codes, warnings policy, and standard JSON mode. - A deterministic local EVM runner for deploy/call scenarios, fixture state, snapshots, traces, revert decoding, and reproducible fork configuration. - Source maps, source-level traces, revert decoding, gas reports, and storage layout diffs. - Incremental/watch builds and a validated content-addressed cache. - LSP, editor extensions, formatter integration, and generated docs on hover. - Foundry/Hardhat adapters, deployment/linking helpers, and explorer verification. Exit criteria: a small team can develop, test, deploy, verify, and debug a JETH project without calling internal compiler APIs. R2: compiler correctness and conformance Goal: make the safety bar durable as the compiler evolves. - Convert the supported matrix into machine-readable capabilities and generated conformance cases. - Expand grammar, type, location, aliasing, inheritance, call, ABI, storage, proxy, and optimizer fuzzing. - Preserve deploy/run/decode comparisons with solc 0.8.35, then define a process for adding future solc baselines. - Add metamorphic tests, invariant tests, test-order shuffling, deterministic seeds, timeouts, and corpus minimization. - Audit optimizer transformations with semantic equivalence and gas baselines. - Prioritize OR lifts by user impact, workaround quality, implementation risk, and proof coverage. Do not optimize for the raw number of accepted constructs. Exit criteria: each acceptance decision can be traced to a capability, tests, and an explicit compiler invariant. R3: language and runtime completeness Goal: close the remaining practical gaps without weakening soundness. - Work through the current \"Still gated\" list in SUPPORTED.md . - Prioritize common contract and ABI shapes, arbitrary contract construction beyond the existing EIP-1167 CREATE/CREATE2 clone surface, function values where safely expressible, and storage-reference library workflows. - Improve import/package boundaries and deterministic dependency resolution. - Add analyzer checks that prevent proxy/storage misuse and common application hazards where the compiler has enough information to prove them. - Add opt-in optimizer passes only with before/after semantic and gas evidence. Exit criteria: representative token, vault, governance, oracle, library, proxy, and diamond projects compile without unsafe workarounds. R4: offline AI verification lab Goal: use local models to increase adversarial coverage without trusting model judgment as a security boundary. Architecture 1. A local LLM, SLM, or reasoning model proposes tests, mutations, invariants, or audit hypotheses. 2. A grammar-aware generator keeps candidates inside a chosen language region. 3. JETH and solc compile equivalent programs. 4. A local EVM deploys and executes both with generated calls and state. 5. An oracle compares bytes, logs, slots, gas classes, and diagnostics. 6. A reducer produces a minimal permanent regression. Model strategy - Begin with retrieval, constrained generation, and tool feedback around a strong local base model. - Build a normalized corpus of fixes, rejects, parity mirrors, diagnostics, and minimized failures. - Fine-tune only after evaluation is stable and leakage-resistant. - Maintain held-out bug families and score reproduction rate, unique findings, false-positive burden, reduction quality, and compute cost. - Let models propose. Let deterministic execution and compiler invariants decide. Exit criteria: the lab repeatedly finds novel, reproducible issues on held-out campaigns and adds minimized tests without creating flaky or unverifiable claims. R5: audited numerical packages Goal: provide useful on-chain math with explicit semantics and bounded risk. First package - full-precision multiplication/division; - signed and unsigned fixed-point types; - explicit floor, ceil, toward-zero, and nearest rounding; - saturating and checked variants where the distinction is visible in the name; - units and range-constrained branded types; - roots, powers, logarithms, and exponentials with documented error bounds; - vector and statistics helpers only after gas and overflow analysis. Calculus-like functionality Integrals, derivatives, and differential methods should not emulate real numbers implicitly. Choose one of two explicit models: - compile-time symbolic transformation for supported expressions; or - runtime numerical approximation with fixed-point input, a stated algorithm, domain restrictions, convergence conditions, maximum error, iteration cap, rounding mode, and worst-case gas. Keep numerical packages outside the compiler core, version them separately, and commission specialized audits. The compiler may later add verified intrinsics for proven hot paths without changing package-level semantics. Exit criteria: every function has a precise numeric contract, reference vectors, differential tests, gas bounds, and independent review. R6: production trust and ecosystem Goal: make JETH credible for high-value deployment. - Independent compiler, CLI, standard-library, and generated-proxy audits. - Public bug bounty and transparent advisories. - Long-running fuzzing infrastructure with retained corpora and seeds. - Reproducible release ceremony and signed release index. - Stable documentation website with version switching and searchable diagnostics. - Reference projects, migration guides, training material, and maintainer policy. - Governance for language proposals and backwards compatibility. Exit criteria: production users can evaluate exactly what was audited, reproduce their compiler, understand compatibility, and receive fixes through a documented security lifecycle. What not to do - Do not market the compiler or a math package as fully safe because tests pass. - Do not let an LLM approve code, diagnostics, or security findings without an executable oracle and human review. - Do not lift an OR solely to increase feature count. - Do not publish a package while license, artifact, or version metadata conflict. - Do not make optimizer changes without semantic regression and gas evidence.","path":"project/roadmap/"},{"title":"Repository overview","group":"Project","text":"JETH ! a16k organization logo docs/assets/a16k-avatar-logo.png JETH is a smart-contract language with a strict TypeScript-shaped syntax and Solidity-compatible EVM semantics. It compiles JETH source to typed IR, Yul, ABI metadata, and EVM bytecode through solc . Built by @a16k-lab https://github.com/a16k-lab . The project follows one non-negotiable compiler rule: a clean rejection is better than accepted code that produces the wrong behavior. Accepted programs are tested against Solidity 0.8.35 at deployment and runtime, including return data, revert data, event logs, and raw storage slots. JETH is pre-release software. It has a large differential test suite and zero known miscompiles at the current revision, but it has not yet completed an independent production security audit or a stable public release process. Do not treat it as production-ready until the release gates below are complete. Why JETH - Familiar TypeScript-shaped syntax without JavaScript runtime semantics. - Checked EVM arithmetic, Solidity-compatible storage, ABI, and revert behavior. - Compile-time mutability inference and a deliberately small visibility model. - Branded value types, exhaustive switches, generics, named/default arguments, struct spread, for...of , and an EIP-1153 @nonReentrant guard. - Native support for interfaces, inheritance, libraries, proxies, beacons, and multiple EIP-2535 diamond storage models. - A safety-first compiler policy: unsupported shapes must fail loudly at compile time instead of falling through to partial code generation. Quick start JETH currently requires Node.js 22 and uses the compiler directly from the repository. Write compiler artifacts to a directory: The public package and standalone binary are roadmap items. Until they ship, the repository command above is the supported entry point. A first contract A bare class is a deployed contract. A bare field is contract storage. External<T exposes a method through the ABI, while get marks a read-only value-returning entry. The compiler infers whether that entry is pure or view . this.count += 1n is checked uint256 arithmetic. It reverts with Panic 0x11 on overflow; it is not JavaScript addition. Current status The current compiler supports a broad Solidity-compatible surface: - value types, enums, branded newtypes, arrays, mappings, structs, tuples, bytes, strings, events, custom errors, and checked/unchecked arithmetic; - constructors, immutable fields, modifiers, inheritance, abstract contracts, interfaces, internal and externally linked libraries; - ABI v2 encoding and decoding, external calls, try / catch , calldata slices, hashing, signature recovery, and EVM precompiles; - EIP-1167 clones, ERC-1967 and transparent proxies, UUPS, beacons, and three EIP-2535 diamond storage models; - compile-time generics, exhaustive switches, named/default arguments, struct spread, for...of , mutability inference, and transient-storage reentrancy protection. The full acceptance matrix and every deliberate compile-time gate live in SUPPORTED.md SUPPORTED.md . JETH currently has more than 5,000 tests across 502 test files, with extensive runtime differential checks against solc 0.8.35. Test count alone is not a security audit; the production gates below remain mandatory. Documentation The standalone HTML language book starts at docs/book/index.html docs/book/index.html . Its Markdown sources live in docs/guide docs/guide/README.md : - Getting started docs/guide/getting-started.md - Source units and imports docs/guide/language/source-units.md - Structure of a contract docs/guide/language/contract-structure.md - Types and data locations docs/guide/language-reference.md - Expressions and control flow docs/guide/language/expressions.md - Functions and composition docs/guide/language/functions.md - Contract ABI specification docs/guide/internals/abi.md - Storage layout docs/guide/internals/storage-layout.md - Examples docs/guide/examples.md - Security considerations docs/guide/security/considerations.md - Compiler correctness model docs/guide/security/compiler-correctness.md - Compiler and tooling docs/guide/compiler-and-tooling.md - Product roadmap docs/guide/roadmap.md Rebuild the HTML book after editing its Markdown sources: The existing files directly under docs/ are engineering design notes, audit records, and implementation specifications. They support compiler development but are not the recommended learning path. Roadmap to a production release The detailed roadmap is maintained in docs/guide/roadmap.md docs/guide/roadmap.md . The short version is ordered by release risk, not novelty. 1. Productize the compiler and CLI - Publish a real jethc package and standalone binary with a stable versioned command surface. - Add project builds, multi-file input, a config file, incremental/watch mode, deterministic artifact directories, and a content-addressed compile cache. - Add structured JSON diagnostics, stable exit codes, standard JSON input/output, source maps, contract selection, optimizer/EVM target controls, and dependency metadata. - Integrate with Foundry, Hardhat, deployment scripts, explorer verification, editor diagnostics, and an LSP. 2. Preserve the correctness bar while expanding the language - Continue differential and adversarial testing for every accepted source shape. - Squash high-value over-rejections only when the lifted path is proven at deploy, run, and decode time. A safe reject remains preferable to a speculative lift. - Strengthen analyzer reachability, aliasing, data-location, ABI, storage-layout, and optimizer invariants with generated conformance tests. - Add the remaining useful compiler features from SUPPORTED.md , with priority based on real contract demand instead of surface-area counts. - Build optimizer validation around semantic equivalence, gas snapshots, and reproducible before/after artifacts. 3. Build a local AI-assisted verification lab - Use an offline model as a test and exploit-candidate proposer, never as the source of truth for compiler correctness or contract safety. - Pair it with deterministic parsers, the JETH analyzer, solc 0.8.35, deploy/run differential oracles, fuzzers, and minimized reproducible witnesses. - First collect a high-quality corpus of accepted/rejected parity cases, known compiler bugs, diagnostics, and reductions. Fine-tune only after the dataset and evaluation harness are stable. - Require every model-generated claim to produce executable evidence. Security findings without a reproducer remain hypotheses. 4. Ship safe numerical libraries as separately audited packages - Start with integer utilities, full-precision mulDiv , fixed-point types, explicit rounding modes, unit/range types, bounds, and domain checks. - Treat transcendental functions, numerical integration, differentiation, and approximation as error-bounded algorithms with explicit precision and gas contracts. - Prefer compile-time symbolic simplification where possible. EVM runtime math must never pretend to have floating-point or real-number semantics. - Keep the math package separate from the compiler core and audit it independently. Avoid claims such as \"super safe\" until a precise threat model and external review support them. 5. Complete the release and security system - Define semantic versioning, changelogs, migration notes, compatibility policy, and a frozen language-spec version for each compiler release. - Produce reproducible builds, signed release artifacts, checksums, an SBOM, and provenance through CI. - Add a public security policy, private disclosure channel, bug bounty, external compiler audit, standard-library audits, and an incident-response process. - Maintain a public conformance corpus and a machine-readable list of supported features and deliberate rejection gates. - Resolve release metadata before publication, including the current mismatch between the GPLv3 LICENSE file and the MIT value in package.json . Compiler pipeline Path Responsibility --- --- src/parser.ts Source parsing and JETH syntax extraction src/validator.ts TypeScript-subset validation src/typeresolver.ts Source annotations to JETH types src/analyzer.ts Semantic analysis and typed IR construction src/layout.ts Solidity-compatible storage planning src/abi.ts ABI metadata generation src/yul.ts Yul generation, codecs, arithmetic, and storage operations src/solc.ts Yul compilation through solc-js src/compile.ts Pipeline and multi-artifact orchestration src/evm.ts Local EVM execution harness src/cli.ts Current jethc command-line entry Development checks Compiler changes should include focused regression pins and, when behavior has a Solidity equivalent, deploy/run/decode comparisons against solc. The suite should also pass with shuffled test-file order to detect hidden global state and flakes. License See LICENSE LICENSE . The repository must resolve the license identifier mismatch in package.json before the first public package release.","path":"project/overview/"}];
